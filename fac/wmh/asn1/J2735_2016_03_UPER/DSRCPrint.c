/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.2.4, Date: 06-Mar-2019.
 */
#include "DSRC.h"
#include "rtsrc/rtPrint.h"
#include "rtxsrc/rtxCommon.h"

void asn1Print_PartII_Id (const char* name, const PartII_Id* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_Acceleration (const char* name, const Acceleration* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_AccelerationConfidence 
   (const char* name, const AccelerationConfidence* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("accl-100-00\n"); break;
      case 2: printf ("accl-010-00\n"); break;
      case 3: printf ("accl-005-00\n"); break;
      case 4: printf ("accl-001-00\n"); break;
      case 5: printf ("accl-000-10\n"); break;
      case 6: printf ("accl-000-05\n"); break;
      case 7: printf ("accl-000-01\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_AdvisorySpeedType 
   (const char* name, const AdvisorySpeedType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("none\n"); break;
      case 1: printf ("greenwave\n"); break;
      case 2: printf ("ecoDrive\n"); break;
      case 3: printf ("transit\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_AllowedManeuvers 
   (const char* name, const AllowedManeuvers* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_AmbientAirPressure 
   (const char* name, const AmbientAirPressure* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_AmbientAirTemperature 
   (const char* name, const AmbientAirTemperature* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_Angle (const char* name, const Angle* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_AnimalPropelledType 
   (const char* name, const AnimalPropelledType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("otherTypes\n"); break;
      case 2: printf ("animalMounted\n"); break;
      case 3: printf ("animalDrawnCarriage\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_AnimalType (const char* name, const AnimalType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("serviceUse\n"); break;
      case 2: printf ("pet\n"); break;
      case 3: printf ("farm\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_AntiLockBrakeStatus 
   (const char* name, const AntiLockBrakeStatus* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("off\n"); break;
      case 2: printf ("on\n"); break;
      case 3: printf ("engaged\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_ApproachID (const char* name, const ApproachID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_Attachment (const char* name, const Attachment* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("stroller\n"); break;
      case 2: printf ("bicycleTrailer\n"); break;
      case 3: printf ("cart\n"); break;
      case 4: printf ("wheelchair\n"); break;
      case 5: printf ("otherWalkAssistAttachments\n"); break;
      case 6: printf ("pet\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_AttachmentRadius 
   (const char* name, const AttachmentRadius* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_AuxiliaryBrakeStatus 
   (const char* name, const AuxiliaryBrakeStatus* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("off\n"); break;
      case 2: printf ("on\n"); break;
      case 3: printf ("reserved\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_BasicVehicleClass 
   (const char* name, const BasicVehicleClass* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_BasicVehicleRole 
   (const char* name, const BasicVehicleRole* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("basicVehicle\n"); break;
      case 1: printf ("publicTransport\n"); break;
      case 2: printf ("specialTransport\n"); break;
      case 3: printf ("dangerousGoods\n"); break;
      case 4: printf ("roadWork\n"); break;
      case 5: printf ("roadRescue\n"); break;
      case 6: printf ("emergency\n"); break;
      case 7: printf ("safetyCar\n"); break;
      case 8: printf ("none-unknown\n"); break;
      case 9: printf ("truck\n"); break;
      case 10: printf ("motorcycle\n"); break;
      case 11: printf ("roadSideSource\n"); break;
      case 12: printf ("police\n"); break;
      case 13: printf ("fire\n"); break;
      case 14: printf ("ambulance\n"); break;
      case 15: printf ("dot\n"); break;
      case 16: printf ("transit\n"); break;
      case 17: printf ("slowMoving\n"); break;
      case 18: printf ("stopNgo\n"); break;
      case 19: printf ("cyclist\n"); break;
      case 20: printf ("pedestrian\n"); break;
      case 21: printf ("nonMotorized\n"); break;
      case 22: printf ("military\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_BrakeAppliedPressure 
   (const char* name, const BrakeAppliedPressure* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("minPressure\n"); break;
      case 2: printf ("bkLvl-2\n"); break;
      case 3: printf ("bkLvl-3\n"); break;
      case 4: printf ("bkLvl-4\n"); break;
      case 5: printf ("bkLvl-5\n"); break;
      case 6: printf ("bkLvl-6\n"); break;
      case 7: printf ("bkLvl-7\n"); break;
      case 8: printf ("bkLvl-8\n"); break;
      case 9: printf ("bkLvl-9\n"); break;
      case 10: printf ("bkLvl-10\n"); break;
      case 11: printf ("bkLvl-11\n"); break;
      case 12: printf ("bkLvl-12\n"); break;
      case 13: printf ("bkLvl-13\n"); break;
      case 14: printf ("bkLvl-14\n"); break;
      case 15: printf ("maxPressure\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_BrakeAppliedStatus 
   (const char* name, const BrakeAppliedStatus* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 8), pvalue->data);
}

void asn1Print_BrakeBoostApplied 
   (const char* name, const BrakeBoostApplied* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("off\n"); break;
      case 2: printf ("on\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_BumperHeight (const char* name, const BumperHeight* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_CoarseHeading (const char* name, const CoarseHeading* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_CodeWord (const char* name, const CodeWord* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_CoefficientOfFriction 
   (const char* name, const CoefficientOfFriction* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_Confidence (const char* name, const Confidence* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_Count (const char* name, const Count* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DDay (const char* name, const DDay* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DeltaAngle (const char* name, const DeltaAngle* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_DeltaTime (const char* name, const DeltaTime* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_DescriptiveName (const char* name, const DescriptiveName pvalue)
{
   rtxPrintIndent ();
   rtxPrintCharStr (name, pvalue);
}

void asn1Print_DHour (const char* name, const DHour* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DirectionOfUse (const char* name, const DirectionOfUse* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("forward\n"); break;
      case 2: printf ("reverse\n"); break;
      case 3: printf ("both\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_DistanceUnits (const char* name, const DistanceUnits* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("centimeter\n"); break;
      case 1: printf ("cm2-5\n"); break;
      case 2: printf ("decimeter\n"); break;
      case 3: printf ("meter\n"); break;
      case 4: printf ("kilometer\n"); break;
      case 5: printf ("foot\n"); break;
      case 6: printf ("yard\n"); break;
      case 7: printf ("mile\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_DMinute (const char* name, const DMinute* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DMonth (const char* name, const DMonth* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DOffset (const char* name, const DOffset* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_DrivenLineOffsetLg 
   (const char* name, const DrivenLineOffsetLg* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_DrivenLineOffsetSm 
   (const char* name, const DrivenLineOffsetSm* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_DrivingWheelAngle 
   (const char* name, const DrivingWheelAngle* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_DSecond (const char* name, const DSecond* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DSRCmsgID (const char* name, const DSRCmsgID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_Duration (const char* name, const Duration* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DYear (const char* name, const DYear* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_ElevationConfidence 
   (const char* name, const ElevationConfidence* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("elev-500-00\n"); break;
      case 2: printf ("elev-200-00\n"); break;
      case 3: printf ("elev-100-00\n"); break;
      case 4: printf ("elev-050-00\n"); break;
      case 5: printf ("elev-020-00\n"); break;
      case 6: printf ("elev-010-00\n"); break;
      case 7: printf ("elev-005-00\n"); break;
      case 8: printf ("elev-002-00\n"); break;
      case 9: printf ("elev-001-00\n"); break;
      case 10: printf ("elev-000-50\n"); break;
      case 11: printf ("elev-000-20\n"); break;
      case 12: printf ("elev-000-10\n"); break;
      case 13: printf ("elev-000-05\n"); break;
      case 14: printf ("elev-000-02\n"); break;
      case 15: printf ("elev-000-01\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_Elevation (const char* name, const Elevation* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_Extent (const char* name, const Extent* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("useInstantlyOnly\n"); break;
      case 1: printf ("useFor3meters\n"); break;
      case 2: printf ("useFor10meters\n"); break;
      case 3: printf ("useFor50meters\n"); break;
      case 4: printf ("useFor100meters\n"); break;
      case 5: printf ("useFor500meters\n"); break;
      case 6: printf ("useFor1000meters\n"); break;
      case 7: printf ("useFor5000meters\n"); break;
      case 8: printf ("useFor10000meters\n"); break;
      case 9: printf ("useFor50000meters\n"); break;
      case 10: printf ("useFor100000meters\n"); break;
      case 11: printf ("useFor500000meters\n"); break;
      case 12: printf ("useFor1000000meters\n"); break;
      case 13: printf ("useFor5000000meters\n"); break;
      case 14: printf ("useFor10000000meters\n"); break;
      case 15: printf ("forever\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_ExteriorLights (const char* name, const ExteriorLights* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceTextExt (name, pvalue->numbits, pvalue->data, sizeof(pvalue->data), pvalue->extdata);
}

void asn1Print_FuelType (const char* name, const FuelType* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_FurtherInfoID (const char* name, const FurtherInfoID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_GNSSstatus (const char* name, const GNSSstatus* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 8), pvalue->data);
}

void asn1Print_GrossDistance (const char* name, const GrossDistance* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_GrossSpeed (const char* name, const GrossSpeed* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_HeadingConfidence 
   (const char* name, const HeadingConfidence* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("prec10deg\n"); break;
      case 2: printf ("prec05deg\n"); break;
      case 3: printf ("prec01deg\n"); break;
      case 4: printf ("prec0-1deg\n"); break;
      case 5: printf ("prec0-05deg\n"); break;
      case 6: printf ("prec0-01deg\n"); break;
      case 7: printf ("prec0-0125deg\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_Heading (const char* name, const Heading* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_HeadingSlice (const char* name, const HeadingSlice* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_IntersectionID (const char* name, const IntersectionID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_IntersectionStatusObject 
   (const char* name, const IntersectionStatusObject* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_IsDolly (const char* name, const IsDolly* pvalue)
{
   rtxPrintIndent ();
   rtxPrintBoolean (name, *pvalue);
}

void asn1Print_Iso3833VehicleType 
   (const char* name, const Iso3833VehicleType* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_ITIStextPhrase (const char* name, const ITIStextPhrase pvalue)
{
   rtxPrintIndent ();
   rtxPrintCharStr (name, pvalue);
}

void asn1Print_AxleLocation (const char* name, const AxleLocation* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_AxleWeight (const char* name, const AxleWeight* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_CargoWeight (const char* name, const CargoWeight* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DriveAxleLiftAirPressure 
   (const char* name, const DriveAxleLiftAirPressure* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DriveAxleLocation 
   (const char* name, const DriveAxleLocation* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DriveAxleLubePressure 
   (const char* name, const DriveAxleLubePressure* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_DriveAxleTemperature 
   (const char* name, const DriveAxleTemperature* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_SteeringAxleLubePressure 
   (const char* name, const SteeringAxleLubePressure* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_SteeringAxleTemperature 
   (const char* name, const SteeringAxleTemperature* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_TireLeakageRate 
   (const char* name, const TireLeakageRate* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_TireLocation (const char* name, const TireLocation* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_TirePressureThresholdDetection 
   (const char* name, const TirePressureThresholdDetection* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("noData\n"); break;
      case 1: printf ("overPressure\n"); break;
      case 2: printf ("noWarningPressure\n"); break;
      case 3: printf ("underPressure\n"); break;
      case 4: printf ("extremeUnderPressure\n"); break;
      case 5: printf ("undefined\n"); break;
      case 6: printf ("errorIndicator\n"); break;
      case 7: printf ("notAvailable\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_TirePressure (const char* name, const TirePressure* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_TireTemp (const char* name, const TireTemp* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_TrailerWeight (const char* name, const TrailerWeight* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_WheelEndElectFault 
   (const char* name, const WheelEndElectFault* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("isOk\n"); break;
      case 1: printf ("isNotDefined\n"); break;
      case 2: printf ("isError\n"); break;
      case 3: printf ("isNotSupported\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_WheelSensorStatus 
   (const char* name, const WheelSensorStatus* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("off\n"); break;
      case 1: printf ("on\n"); break;
      case 2: printf ("notDefined\n"); break;
      case 3: printf ("notSupported\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_LaneAttributes_Barrier 
   (const char* name, const LaneAttributes_Barrier* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_LaneAttributes_Bike 
   (const char* name, const LaneAttributes_Bike* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_LaneAttributes_Crosswalk 
   (const char* name, const LaneAttributes_Crosswalk* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_LaneAttributes_Parking 
   (const char* name, const LaneAttributes_Parking* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_LaneAttributes_Sidewalk 
   (const char* name, const LaneAttributes_Sidewalk* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_LaneAttributes_Striping 
   (const char* name, const LaneAttributes_Striping* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_LaneAttributes_TrackedVehicle 
   (const char* name, const LaneAttributes_TrackedVehicle* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_LaneAttributes_Vehicle 
   (const char* name, const LaneAttributes_Vehicle* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceTextExt (name, pvalue->numbits, pvalue->data, sizeof(pvalue->data), pvalue->extdata);
}

void asn1Print_LaneConnectionID 
   (const char* name, const LaneConnectionID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_LaneDirection (const char* name, const LaneDirection* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 8), pvalue->data);
}

void asn1Print_LaneID (const char* name, const LaneID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_LaneSharing (const char* name, const LaneSharing* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_LaneWidth (const char* name, const LaneWidth* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_Latitude (const char* name, const Latitude* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_LayerID (const char* name, const LayerID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_LayerType (const char* name, const LayerType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("none\n"); break;
      case 1: printf ("mixedContent\n"); break;
      case 2: printf ("generalMapData\n"); break;
      case 3: printf ("intersectionData\n"); break;
      case 4: printf ("curveData\n"); break;
      case 5: printf ("roadwaySectionData\n"); break;
      case 6: printf ("parkingAreaData\n"); break;
      case 7: printf ("sharedLaneData\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_LightbarInUse (const char* name, const LightbarInUse* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("notInUse\n"); break;
      case 2: printf ("inUse\n"); break;
      case 3: printf ("yellowCautionLights\n"); break;
      case 4: printf ("schooldBusLights\n"); break;
      case 5: printf ("arrowSignsActive\n"); break;
      case 6: printf ("slowMovingVehicle\n"); break;
      case 7: printf ("freqStops\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_Longitude (const char* name, const Longitude* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_Location_quality 
   (const char* name, const Location_quality* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("loc-qual-bt1m\n"); break;
      case 1: printf ("loc-qual-bt5m\n"); break;
      case 2: printf ("loc-qual-bt12m\n"); break;
      case 3: printf ("loc-qual-bt50m\n"); break;
      case 4: printf ("loc-qual-bt125m\n"); break;
      case 5: printf ("loc-qual-bt500m\n"); break;
      case 6: printf ("loc-qual-bt1250m\n"); break;
      case 7: printf ("loc-qual-unknown\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_Location_tech (const char* name, const Location_tech* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("loc-tech-unknown\n"); break;
      case 1: printf ("loc-tech-GNSS\n"); break;
      case 2: printf ("loc-tech-DGPS\n"); break;
      case 3: printf ("loc-tech-RTK\n"); break;
      case 4: printf ("loc-tech-PPP\n"); break;
      case 5: printf ("loc-tech-drGPS\n"); break;
      case 6: printf ("loc-tech-drDGPS\n"); break;
      case 7: printf ("loc-tech-dr\n"); break;
      case 8: printf ("loc-tech-nav\n"); break;
      case 9: printf ("loc-tech-fault\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_MergeDivergeNodeAngle 
   (const char* name, const MergeDivergeNodeAngle* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_MessageBLOB (const char* name, const MessageBLOB* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_MinuteOfTheYear 
   (const char* name, const MinuteOfTheYear* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_MinutesDuration 
   (const char* name, const MinutesDuration* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_MotorizedPropelledType 
   (const char* name, const MotorizedPropelledType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("otherTypes\n"); break;
      case 2: printf ("wheelChair\n"); break;
      case 3: printf ("bicycle\n"); break;
      case 4: printf ("scooter\n"); break;
      case 5: printf ("selfBalancingDevice\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_MovementPhaseState 
   (const char* name, const MovementPhaseState* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("dark\n"); break;
      case 2: printf ("stop-Then-Proceed\n"); break;
      case 3: printf ("stop-And-Remain\n"); break;
      case 4: printf ("pre-Movement\n"); break;
      case 5: printf ("permissive-Movement-Allowed\n"); break;
      case 6: printf ("protected-Movement-Allowed\n"); break;
      case 7: printf ("permissive-clearance\n"); break;
      case 8: printf ("protected-clearance\n"); break;
      case 9: printf ("caution-Conflicting-Traffic\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_MsgCount (const char* name, const MsgCount* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_MsgCRC (const char* name, const MsgCRC* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_MultiVehicleResponse 
   (const char* name, const MultiVehicleResponse* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("singleVehicle\n"); break;
      case 2: printf ("multiVehicle\n"); break;
      case 3: printf ("reserved\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_MUTCDCode (const char* name, const MUTCDCode* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("none\n"); break;
      case 1: printf ("regulatory\n"); break;
      case 2: printf ("warning\n"); break;
      case 3: printf ("maintenance\n"); break;
      case 4: printf ("motoristService\n"); break;
      case 5: printf ("guide\n"); break;
      case 6: printf ("rec\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_NMEA_MsgType (const char* name, const NMEA_MsgType* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_NMEA_Payload (const char* name, const NMEA_Payload* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_NMEA_Revision (const char* name, const NMEA_Revision* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unknown\n"); break;
      case 1: printf ("reserved\n"); break;
      case 2: printf ("rev1\n"); break;
      case 3: printf ("rev2\n"); break;
      case 4: printf ("rev3\n"); break;
      case 5: printf ("rev4\n"); break;
      case 6: printf ("rev5\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_NodeAttributeLL 
   (const char* name, const NodeAttributeLL* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("reserved\n"); break;
      case 1: printf ("stopLine\n"); break;
      case 2: printf ("roundedCapStyleA\n"); break;
      case 3: printf ("roundedCapStyleB\n"); break;
      case 4: printf ("mergePoint\n"); break;
      case 5: printf ("divergePoint\n"); break;
      case 6: printf ("downstreamStopLine\n"); break;
      case 7: printf ("downstreamStartNode\n"); break;
      case 8: printf ("closedToTraffic\n"); break;
      case 9: printf ("safeIsland\n"); break;
      case 10: printf ("curbPresentAtStepOff\n"); break;
      case 11: printf ("hydrantPresent\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_NodeAttributeXY 
   (const char* name, const NodeAttributeXY* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("reserved\n"); break;
      case 1: printf ("stopLine\n"); break;
      case 2: printf ("roundedCapStyleA\n"); break;
      case 3: printf ("roundedCapStyleB\n"); break;
      case 4: printf ("mergePoint\n"); break;
      case 5: printf ("divergePoint\n"); break;
      case 6: printf ("downstreamStopLine\n"); break;
      case 7: printf ("downstreamStartNode\n"); break;
      case 8: printf ("closedToTraffic\n"); break;
      case 9: printf ("safeIsland\n"); break;
      case 10: printf ("curbPresentAtStepOff\n"); break;
      case 11: printf ("hydrantPresent\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_NumberOfParticipantsInCluster 
   (const char* name, const NumberOfParticipantsInCluster* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("small\n"); break;
      case 2: printf ("medium\n"); break;
      case 3: printf ("large\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_ObjectCount (const char* name, const ObjectCount* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_ObstacleDirection 
   (const char* name, const ObstacleDirection* pvalue)
{
   asn1Print_Angle (name, pvalue);
}

void asn1Print_ObstacleDistance 
   (const char* name, const ObstacleDistance* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_Offset_B09 (const char* name, const Offset_B09* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_Offset_B10 (const char* name, const Offset_B10* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_Offset_B11 (const char* name, const Offset_B11* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_Offset_B12 (const char* name, const Offset_B12* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_Offset_B13 (const char* name, const Offset_B13* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_Offset_B14 (const char* name, const Offset_B14* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_Offset_B16 (const char* name, const Offset_B16* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_OffsetLL_B12 (const char* name, const OffsetLL_B12* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_OffsetLL_B14 (const char* name, const OffsetLL_B14* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_OffsetLL_B16 (const char* name, const OffsetLL_B16* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_OffsetLL_B18 (const char* name, const OffsetLL_B18* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_OffsetLL_B22 (const char* name, const OffsetLL_B22* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_OffsetLL_B24 (const char* name, const OffsetLL_B24* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_PayloadData (const char* name, const PayloadData* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_PedestrianBicycleDetect 
   (const char* name, const PedestrianBicycleDetect* pvalue)
{
   rtxPrintIndent ();
   rtxPrintBoolean (name, *pvalue);
}

void asn1Print_HumanPropelledType 
   (const char* name, const HumanPropelledType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("otherTypes\n"); break;
      case 2: printf ("onFoot\n"); break;
      case 3: printf ("skateboard\n"); break;
      case 4: printf ("pushOrKickScooter\n"); break;
      case 5: printf ("wheelchair\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_PersonalAssistive 
   (const char* name, const PersonalAssistive* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceTextExt (name, pvalue->numbits, pvalue->data, sizeof(pvalue->data), pvalue->extdata);
}

void asn1Print_PersonalClusterRadius 
   (const char* name, const PersonalClusterRadius* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_PersonalCrossingInProgress 
   (const char* name, const PersonalCrossingInProgress* pvalue)
{
   rtxPrintIndent ();
   rtxPrintBoolean (name, *pvalue);
}

void asn1Print_PersonalCrossingRequest 
   (const char* name, const PersonalCrossingRequest* pvalue)
{
   rtxPrintIndent ();
   rtxPrintBoolean (name, *pvalue);
}

void asn1Print_PersonalDeviceUsageState 
   (const char* name, const PersonalDeviceUsageState* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceTextExt (name, pvalue->numbits, pvalue->data, sizeof(pvalue->data), pvalue->extdata);
}

void asn1Print_PersonalDeviceUserType 
   (const char* name, const PersonalDeviceUserType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("aPEDESTRIAN\n"); break;
      case 2: printf ("aPEDALCYCLIST\n"); break;
      case 3: printf ("aPUBLICSAFETYWORKER\n"); break;
      case 4: printf ("anANIMAL\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_PivotingAllowed 
   (const char* name, const PivotingAllowed* pvalue)
{
   rtxPrintIndent ();
   rtxPrintBoolean (name, *pvalue);
}

void asn1Print_PositionConfidence 
   (const char* name, const PositionConfidence* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("a500m\n"); break;
      case 2: printf ("a200m\n"); break;
      case 3: printf ("a100m\n"); break;
      case 4: printf ("a50m\n"); break;
      case 5: printf ("a20m\n"); break;
      case 6: printf ("a10m\n"); break;
      case 7: printf ("a5m\n"); break;
      case 8: printf ("a2m\n"); break;
      case 9: printf ("a1m\n"); break;
      case 10: printf ("a50cm\n"); break;
      case 11: printf ("a20cm\n"); break;
      case 12: printf ("a10cm\n"); break;
      case 13: printf ("a5cm\n"); break;
      case 14: printf ("a2cm\n"); break;
      case 15: printf ("a1cm\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_PrioritizationResponseStatus 
   (const char* name, const PrioritizationResponseStatus* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unknown\n"); break;
      case 1: printf ("requested\n"); break;
      case 2: printf ("processing\n"); break;
      case 3: printf ("watchOtherTraffic\n"); break;
      case 4: printf ("granted\n"); break;
      case 5: printf ("rejected\n"); break;
      case 6: printf ("maxPresence\n"); break;
      case 7: printf ("reserviceLocked\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_Priority (const char* name, const Priority* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_PriorityRequestType 
   (const char* name, const PriorityRequestType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("priorityRequestTypeReserved\n"); break;
      case 1: printf ("priorityRequest\n"); break;
      case 2: printf ("priorityRequestUpdate\n"); break;
      case 3: printf ("priorityCancellation\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_PrivilegedEventFlags 
   (const char* name, const PrivilegedEventFlags* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 16), pvalue->data);
}

void asn1Print_ProbeSegmentNumber 
   (const char* name, const ProbeSegmentNumber* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_PublicSafetyAndRoadWorkerActivity 
   (const char* name, const PublicSafetyAndRoadWorkerActivity* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceTextExt (name, pvalue->numbits, pvalue->data, sizeof(pvalue->data), pvalue->extdata);
}

void asn1Print_PublicSafetyDirectingTrafficSubType 
   (const char* name, const PublicSafetyDirectingTrafficSubType* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceTextExt (name, pvalue->numbits, pvalue->data, sizeof(pvalue->data), pvalue->extdata);
}

void asn1Print_PublicSafetyEventResponderWorkerType 
   (const char* name, const PublicSafetyEventResponderWorkerType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("towOperater\n"); break;
      case 2: printf ("fireAndEMSWorker\n"); break;
      case 3: printf ("aDOTWorker\n"); break;
      case 4: printf ("lawEnforcement\n"); break;
      case 5: printf ("hazmatResponder\n"); break;
      case 6: printf ("animalControlWorker\n"); break;
      case 7: printf ("otherPersonnel\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_RadiusOfCurvature 
   (const char* name, const RadiusOfCurvature* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_Radius_B12 (const char* name, const Radius_B12* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_RainSensor (const char* name, const RainSensor* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("none\n"); break;
      case 1: printf ("lightMist\n"); break;
      case 2: printf ("heavyMist\n"); break;
      case 3: printf ("lightRainOrDrizzle\n"); break;
      case 4: printf ("rain\n"); break;
      case 5: printf ("moderateRain\n"); break;
      case 6: printf ("heavyRain\n"); break;
      case 7: printf ("heavyDownpour\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_RegionId (const char* name, const RegionId* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_RequestedItem (const char* name, const RequestedItem* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("reserved\n"); break;
      case 1: printf ("itemA\n"); break;
      case 2: printf ("itemB\n"); break;
      case 3: printf ("itemC\n"); break;
      case 4: printf ("itemD\n"); break;
      case 5: printf ("itemE\n"); break;
      case 6: printf ("itemF\n"); break;
      case 7: printf ("itemG\n"); break;
      case 8: printf ("itemI\n"); break;
      case 9: printf ("itemJ\n"); break;
      case 10: printf ("itemK\n"); break;
      case 11: printf ("itemL\n"); break;
      case 12: printf ("itemM\n"); break;
      case 13: printf ("itemN\n"); break;
      case 14: printf ("itemO\n"); break;
      case 15: printf ("itemP\n"); break;
      case 16: printf ("itemQ\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_RequestID (const char* name, const RequestID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_RequestImportanceLevel 
   (const char* name, const RequestImportanceLevel* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("requestImportanceLevelUnKnown\n"); break;
      case 1: printf ("requestImportanceLevel1\n"); break;
      case 2: printf ("requestImportanceLevel2\n"); break;
      case 3: printf ("requestImportanceLevel3\n"); break;
      case 4: printf ("requestImportanceLevel4\n"); break;
      case 5: printf ("requestImportanceLevel5\n"); break;
      case 6: printf ("requestImportanceLevel6\n"); break;
      case 7: printf ("requestImportanceLevel7\n"); break;
      case 8: printf ("requestImportanceLevel8\n"); break;
      case 9: printf ("requestImportanceLevel9\n"); break;
      case 10: printf ("requestImportanceLevel10\n"); break;
      case 11: printf ("requestImportanceLevel11\n"); break;
      case 12: printf ("requestImportanceLevel12\n"); break;
      case 13: printf ("requestImportanceLevel13\n"); break;
      case 14: printf ("requestImportanceLevel14\n"); break;
      case 15: printf ("requestImportanceReserved\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_RequestSubRole (const char* name, const RequestSubRole* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("requestSubRoleUnKnown\n"); break;
      case 1: printf ("requestSubRole1\n"); break;
      case 2: printf ("requestSubRole2\n"); break;
      case 3: printf ("requestSubRole3\n"); break;
      case 4: printf ("requestSubRole4\n"); break;
      case 5: printf ("requestSubRole5\n"); break;
      case 6: printf ("requestSubRole6\n"); break;
      case 7: printf ("requestSubRole7\n"); break;
      case 8: printf ("requestSubRole8\n"); break;
      case 9: printf ("requestSubRole9\n"); break;
      case 10: printf ("requestSubRole10\n"); break;
      case 11: printf ("requestSubRole11\n"); break;
      case 12: printf ("requestSubRole12\n"); break;
      case 13: printf ("requestSubRole13\n"); break;
      case 14: printf ("requestSubRole14\n"); break;
      case 15: printf ("requestSubRoleReserved\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_ResponseType (const char* name, const ResponseType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("notInUseOrNotEquipped\n"); break;
      case 1: printf ("emergency\n"); break;
      case 2: printf ("nonEmergency\n"); break;
      case 3: printf ("pursuit\n"); break;
      case 4: printf ("stationary\n"); break;
      case 5: printf ("slowMoving\n"); break;
      case 6: printf ("stopAndGoMovement\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_RestrictionAppliesTo 
   (const char* name, const RestrictionAppliesTo* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("none\n"); break;
      case 1: printf ("equippedTransit\n"); break;
      case 2: printf ("equippedTaxis\n"); break;
      case 3: printf ("equippedOther\n"); break;
      case 4: printf ("emissionCompliant\n"); break;
      case 5: printf ("equippedBicycle\n"); break;
      case 6: printf ("weightCompliant\n"); break;
      case 7: printf ("heightCompliant\n"); break;
      case 8: printf ("pedestrians\n"); break;
      case 9: printf ("slowMovingPersons\n"); break;
      case 10: printf ("wheelchairUsers\n"); break;
      case 11: printf ("visualDisabilities\n"); break;
      case 12: printf ("audioDisabilities\n"); break;
      case 13: printf ("otherUnknownDisabilities\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_RestrictionClassID 
   (const char* name, const RestrictionClassID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_RoadRegulatorID 
   (const char* name, const RoadRegulatorID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_RoadSegmentID (const char* name, const RoadSegmentID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_RoadwayCrownAngle 
   (const char* name, const RoadwayCrownAngle* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_RTCM_Revision (const char* name, const RTCM_Revision* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unknown\n"); break;
      case 1: printf ("rtcmRev2\n"); break;
      case 2: printf ("rtcmRev3\n"); break;
      case 3: printf ("reserved\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_RTCMmessage (const char* name, const RTCMmessage* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_Scale_B12 (const char* name, const Scale_B12* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_SecondOfTime (const char* name, const SecondOfTime* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_SegmentAttributeLL 
   (const char* name, const SegmentAttributeLL* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("reserved\n"); break;
      case 1: printf ("doNotBlock\n"); break;
      case 2: printf ("whiteLine\n"); break;
      case 3: printf ("mergingLaneLeft\n"); break;
      case 4: printf ("mergingLaneRight\n"); break;
      case 5: printf ("curbOnLeft\n"); break;
      case 6: printf ("curbOnRight\n"); break;
      case 7: printf ("loadingzoneOnLeft\n"); break;
      case 8: printf ("loadingzoneOnRight\n"); break;
      case 9: printf ("turnOutPointOnLeft\n"); break;
      case 10: printf ("turnOutPointOnRight\n"); break;
      case 11: printf ("adjacentParkingOnLeft\n"); break;
      case 12: printf ("adjacentParkingOnRight\n"); break;
      case 13: printf ("adjacentBikeLaneOnLeft\n"); break;
      case 14: printf ("adjacentBikeLaneOnRight\n"); break;
      case 15: printf ("sharedBikeLane\n"); break;
      case 16: printf ("bikeBoxInFront\n"); break;
      case 17: printf ("transitStopOnLeft\n"); break;
      case 18: printf ("transitStopOnRight\n"); break;
      case 19: printf ("transitStopInLane\n"); break;
      case 20: printf ("sharedWithTrackedVehicle\n"); break;
      case 21: printf ("safeIsland\n"); break;
      case 22: printf ("lowCurbsPresent\n"); break;
      case 23: printf ("rumbleStripPresent\n"); break;
      case 24: printf ("audibleSignalingPresent\n"); break;
      case 25: printf ("adaptiveTimingPresent\n"); break;
      case 26: printf ("rfSignalRequestPresent\n"); break;
      case 27: printf ("partialCurbIntrusion\n"); break;
      case 28: printf ("taperToLeft\n"); break;
      case 29: printf ("taperToRight\n"); break;
      case 30: printf ("taperToCenterLine\n"); break;
      case 31: printf ("parallelParking\n"); break;
      case 32: printf ("headInParking\n"); break;
      case 33: printf ("freeParking\n"); break;
      case 34: printf ("timeRestrictionsOnParking\n"); break;
      case 35: printf ("costToPark\n"); break;
      case 36: printf ("midBlockCurbPresent\n"); break;
      case 37: printf ("unEvenPavementPresent\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_SegmentAttributeXY 
   (const char* name, const SegmentAttributeXY* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("reserved\n"); break;
      case 1: printf ("doNotBlock\n"); break;
      case 2: printf ("whiteLine\n"); break;
      case 3: printf ("mergingLaneLeft\n"); break;
      case 4: printf ("mergingLaneRight\n"); break;
      case 5: printf ("curbOnLeft\n"); break;
      case 6: printf ("curbOnRight\n"); break;
      case 7: printf ("loadingzoneOnLeft\n"); break;
      case 8: printf ("loadingzoneOnRight\n"); break;
      case 9: printf ("turnOutPointOnLeft\n"); break;
      case 10: printf ("turnOutPointOnRight\n"); break;
      case 11: printf ("adjacentParkingOnLeft\n"); break;
      case 12: printf ("adjacentParkingOnRight\n"); break;
      case 13: printf ("adjacentBikeLaneOnLeft\n"); break;
      case 14: printf ("adjacentBikeLaneOnRight\n"); break;
      case 15: printf ("sharedBikeLane\n"); break;
      case 16: printf ("bikeBoxInFront\n"); break;
      case 17: printf ("transitStopOnLeft\n"); break;
      case 18: printf ("transitStopOnRight\n"); break;
      case 19: printf ("transitStopInLane\n"); break;
      case 20: printf ("sharedWithTrackedVehicle\n"); break;
      case 21: printf ("safeIsland\n"); break;
      case 22: printf ("lowCurbsPresent\n"); break;
      case 23: printf ("rumbleStripPresent\n"); break;
      case 24: printf ("audibleSignalingPresent\n"); break;
      case 25: printf ("adaptiveTimingPresent\n"); break;
      case 26: printf ("rfSignalRequestPresent\n"); break;
      case 27: printf ("partialCurbIntrusion\n"); break;
      case 28: printf ("taperToLeft\n"); break;
      case 29: printf ("taperToRight\n"); break;
      case 30: printf ("taperToCenterLine\n"); break;
      case 31: printf ("parallelParking\n"); break;
      case 32: printf ("headInParking\n"); break;
      case 33: printf ("freeParking\n"); break;
      case 34: printf ("timeRestrictionsOnParking\n"); break;
      case 35: printf ("costToPark\n"); break;
      case 36: printf ("midBlockCurbPresent\n"); break;
      case 37: printf ("unEvenPavementPresent\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_SemiMajorAxisAccuracy 
   (const char* name, const SemiMajorAxisAccuracy* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_SemiMajorAxisOrientation 
   (const char* name, const SemiMajorAxisOrientation* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_SemiMinorAxisAccuracy 
   (const char* name, const SemiMinorAxisAccuracy* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_SignalGroupID (const char* name, const SignalGroupID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_SignalReqScheme 
   (const char* name, const SignalReqScheme* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_SignPrority (const char* name, const SignPrority* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_SirenInUse (const char* name, const SirenInUse* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("notInUse\n"); break;
      case 2: printf ("inUse\n"); break;
      case 3: printf ("reserved\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_SpeedAdvice (const char* name, const SpeedAdvice* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_SpeedConfidence 
   (const char* name, const SpeedConfidence* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("prec100ms\n"); break;
      case 2: printf ("prec10ms\n"); break;
      case 3: printf ("prec5ms\n"); break;
      case 4: printf ("prec1ms\n"); break;
      case 5: printf ("prec0-1ms\n"); break;
      case 6: printf ("prec0-05ms\n"); break;
      case 7: printf ("prec0-01ms\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_SpeedLimitType (const char* name, const SpeedLimitType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unknown\n"); break;
      case 1: printf ("maxSpeedInSchoolZone\n"); break;
      case 2: printf ("maxSpeedInSchoolZoneWhenChildrenArePresent\n"); break;
      case 3: printf ("maxSpeedInConstructionZone\n"); break;
      case 4: printf ("vehicleMinSpeed\n"); break;
      case 5: printf ("vehicleMaxSpeed\n"); break;
      case 6: printf ("vehicleNightMaxSpeed\n"); break;
      case 7: printf ("truckMinSpeed\n"); break;
      case 8: printf ("truckMaxSpeed\n"); break;
      case 9: printf ("truckNightMaxSpeed\n"); break;
      case 10: printf ("vehiclesWithTrailersMinSpeed\n"); break;
      case 11: printf ("vehiclesWithTrailersMaxSpeed\n"); break;
      case 12: printf ("vehiclesWithTrailersNightMaxSpeed\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_SpeedProfileMeasurement 
   (const char* name, const SpeedProfileMeasurement* pvalue)
{
   asn1Print_GrossSpeed (name, pvalue);
}

void asn1Print_Speed (const char* name, const Speed* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_SSPindex (const char* name, const SSPindex* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_StabilityControlStatus 
   (const char* name, const StabilityControlStatus* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("off\n"); break;
      case 2: printf ("on\n"); break;
      case 3: printf ("engaged\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_StationID (const char* name, const StationID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_SteeringWheelAngleConfidence 
   (const char* name, const SteeringWheelAngleConfidence* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("prec2deg\n"); break;
      case 2: printf ("prec1deg\n"); break;
      case 3: printf ("prec0-02deg\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_SteeringWheelAngleRateOfChange 
   (const char* name, const SteeringWheelAngleRateOfChange* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_SteeringWheelAngle 
   (const char* name, const SteeringWheelAngle* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_SunSensor (const char* name, const SunSensor* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_TemporaryID (const char* name, const TemporaryID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_TermDistance (const char* name, const TermDistance* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_TermTime (const char* name, const TermTime* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_ThrottleConfidence 
   (const char* name, const ThrottleConfidence* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("prec10percent\n"); break;
      case 2: printf ("prec1percent\n"); break;
      case 3: printf ("prec0-5percent\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_ThrottlePosition 
   (const char* name, const ThrottlePosition* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_TimeConfidence (const char* name, const TimeConfidence* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("time-100-000\n"); break;
      case 2: printf ("time-050-000\n"); break;
      case 3: printf ("time-020-000\n"); break;
      case 4: printf ("time-010-000\n"); break;
      case 5: printf ("time-002-000\n"); break;
      case 6: printf ("time-001-000\n"); break;
      case 7: printf ("time-000-500\n"); break;
      case 8: printf ("time-000-200\n"); break;
      case 9: printf ("time-000-100\n"); break;
      case 10: printf ("time-000-050\n"); break;
      case 11: printf ("time-000-020\n"); break;
      case 12: printf ("time-000-010\n"); break;
      case 13: printf ("time-000-005\n"); break;
      case 14: printf ("time-000-002\n"); break;
      case 15: printf ("time-000-001\n"); break;
      case 16: printf ("time-000-000-5\n"); break;
      case 17: printf ("time-000-000-2\n"); break;
      case 18: printf ("time-000-000-1\n"); break;
      case 19: printf ("time-000-000-05\n"); break;
      case 20: printf ("time-000-000-02\n"); break;
      case 21: printf ("time-000-000-01\n"); break;
      case 22: printf ("time-000-000-005\n"); break;
      case 23: printf ("time-000-000-002\n"); break;
      case 24: printf ("time-000-000-001\n"); break;
      case 25: printf ("time-000-000-000-5\n"); break;
      case 26: printf ("time-000-000-000-2\n"); break;
      case 27: printf ("time-000-000-000-1\n"); break;
      case 28: printf ("time-000-000-000-05\n"); break;
      case 29: printf ("time-000-000-000-02\n"); break;
      case 30: printf ("time-000-000-000-01\n"); break;
      case 31: printf ("time-000-000-000-005\n"); break;
      case 32: printf ("time-000-000-000-002\n"); break;
      case 33: printf ("time-000-000-000-001\n"); break;
      case 34: printf ("time-000-000-000-000-5\n"); break;
      case 35: printf ("time-000-000-000-000-2\n"); break;
      case 36: printf ("time-000-000-000-000-1\n"); break;
      case 37: printf ("time-000-000-000-000-05\n"); break;
      case 38: printf ("time-000-000-000-000-02\n"); break;
      case 39: printf ("time-000-000-000-000-01\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_TimeIntervalConfidence 
   (const char* name, const TimeIntervalConfidence* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_TimeMark (const char* name, const TimeMark* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_TimeOffset (const char* name, const TimeOffset* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_TractionControlStatus 
   (const char* name, const TractionControlStatus* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("off\n"); break;
      case 2: printf ("on\n"); break;
      case 3: printf ("engaged\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_TrailerMass (const char* name, const TrailerMass* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_TransitStatus (const char* name, const TransitStatus* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 8), pvalue->data);
}

void asn1Print_TransitVehicleOccupancy 
   (const char* name, const TransitVehicleOccupancy* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("occupancyUnknown\n"); break;
      case 1: printf ("occupancyEmpty\n"); break;
      case 2: printf ("occupancyVeryLow\n"); break;
      case 3: printf ("occupancyLow\n"); break;
      case 4: printf ("occupancyMed\n"); break;
      case 5: printf ("occupancyHigh\n"); break;
      case 6: printf ("occupancyNearlyFull\n"); break;
      case 7: printf ("occupancyFull\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_TransitVehicleStatus 
   (const char* name, const TransitVehicleStatus* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 8), pvalue->data);
}

void asn1Print_TransmissionState 
   (const char* name, const TransmissionState* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("neutral\n"); break;
      case 1: printf ("park\n"); break;
      case 2: printf ("forwardGears\n"); break;
      case 3: printf ("reverseGears\n"); break;
      case 4: printf ("reserved1\n"); break;
      case 5: printf ("reserved2\n"); break;
      case 6: printf ("reserved3\n"); break;
      case 7: printf ("unavailable\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_TravelerInfoType 
   (const char* name, const TravelerInfoType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unknown\n"); break;
      case 1: printf ("advisory\n"); break;
      case 2: printf ("roadSignage\n"); break;
      case 3: printf ("commercialSignage\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_UniqueMSGID (const char* name, const UniqueMSGID* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_URL_Base (const char* name, const URL_Base pvalue)
{
   rtxPrintIndent ();
   rtxPrintCharStr (name, pvalue);
}

void asn1Print_URL_Link (const char* name, const URL_Link pvalue)
{
   rtxPrintIndent ();
   rtxPrintCharStr (name, pvalue);
}

void asn1Print_URL_Short (const char* name, const URL_Short pvalue)
{
   rtxPrintIndent ();
   rtxPrintCharStr (name, pvalue);
}

void asn1Print_UserSizeAndBehaviour 
   (const char* name, const UserSizeAndBehaviour* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceTextExt (name, pvalue->numbits, pvalue->data, sizeof(pvalue->data), pvalue->extdata);
}

void asn1Print_VehicleEventFlags 
   (const char* name, const VehicleEventFlags* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceTextExt (name, pvalue->numbits, pvalue->data, sizeof(pvalue->data), pvalue->extdata);
}

void asn1Print_VehicleHeight (const char* name, const VehicleHeight* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_VehicleLength (const char* name, const VehicleLength* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_VehicleMass (const char* name, const VehicleMass* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_VehicleStatusDeviceTypeTag 
   (const char* name, const VehicleStatusDeviceTypeTag* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unknown\n"); break;
      case 1: printf ("lights\n"); break;
      case 2: printf ("wipers\n"); break;
      case 3: printf ("brakes\n"); break;
      case 4: printf ("stab\n"); break;
      case 5: printf ("trac\n"); break;
      case 6: printf ("abs\n"); break;
      case 7: printf ("sunS\n"); break;
      case 8: printf ("rainS\n"); break;
      case 9: printf ("airTemp\n"); break;
      case 10: printf ("steering\n"); break;
      case 11: printf ("vertAccelThres\n"); break;
      case 12: printf ("vertAccel\n"); break;
      case 13: printf ("hozAccelLong\n"); break;
      case 14: printf ("hozAccelLat\n"); break;
      case 15: printf ("hozAccelCon\n"); break;
      case 16: printf ("accel4way\n"); break;
      case 17: printf ("confidenceSet\n"); break;
      case 18: printf ("obDist\n"); break;
      case 19: printf ("obDirect\n"); break;
      case 20: printf ("yaw\n"); break;
      case 21: printf ("yawRateCon\n"); break;
      case 22: printf ("dateTime\n"); break;
      case 23: printf ("fullPos\n"); break;
      case 24: printf ("position2D\n"); break;
      case 25: printf ("position3D\n"); break;
      case 26: printf ("vehicle\n"); break;
      case 27: printf ("speedHeadC\n"); break;
      case 28: printf ("speedC\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_VehicleType (const char* name, const VehicleType* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("none\n"); break;
      case 1: printf ("unknown\n"); break;
      case 2: printf ("special\n"); break;
      case 3: printf ("moto\n"); break;
      case 4: printf ("car\n"); break;
      case 5: printf ("carOther\n"); break;
      case 6: printf ("bus\n"); break;
      case 7: printf ("axleCnt2\n"); break;
      case 8: printf ("axleCnt3\n"); break;
      case 9: printf ("axleCnt4\n"); break;
      case 10: printf ("axleCnt4Trailer\n"); break;
      case 11: printf ("axleCnt5Trailer\n"); break;
      case 12: printf ("axleCnt6Trailer\n"); break;
      case 13: printf ("axleCnt5MultiTrailer\n"); break;
      case 14: printf ("axleCnt6MultiTrailer\n"); break;
      case 15: printf ("axleCnt7MultiTrailer\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_VehicleWidth (const char* name, const VehicleWidth* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_Velocity (const char* name, const Velocity* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_VerticalAccelerationThreshold 
   (const char* name, const VerticalAccelerationThreshold* pvalue)
{
   rtxPrintIndent ();
   rtPrintBitStrBraceText (name, OS_MIN(pvalue->numbits, 8), pvalue->data);
}

void asn1Print_VerticalAcceleration 
   (const char* name, const VerticalAcceleration* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_VertOffset_B07 (const char* name, const VertOffset_B07* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_VertOffset_B08 (const char* name, const VertOffset_B08* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_VertOffset_B09 (const char* name, const VertOffset_B09* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_VertOffset_B10 (const char* name, const VertOffset_B10* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_VertOffset_B11 (const char* name, const VertOffset_B11* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_VertOffset_B12 (const char* name, const VertOffset_B12* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_VINstring (const char* name, const VINstring* pvalue)
{
   rtxPrintIndent ();
   rtxPrintHexStr (name, OSRTMIN(sizeof(pvalue->data), pvalue->numocts), pvalue->data);
}

void asn1Print_WaitOnStopline (const char* name, const WaitOnStopline* pvalue)
{
   rtxPrintIndent ();
   rtxPrintBoolean (name, *pvalue);
}

void asn1Print_WiperRate (const char* name, const WiperRate* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_WiperStatus (const char* name, const WiperStatus* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("off\n"); break;
      case 2: printf ("intermittent\n"); break;
      case 3: printf ("low\n"); break;
      case 4: printf ("high\n"); break;
      case 5: printf ("washerInUse\n"); break;
      case 6: printf ("automaticPresent\n"); break;
      default: printf ("%u\n", *pvalue);
   }
}

void asn1Print_YawRateConfidence 
   (const char* name, const YawRateConfidence* pvalue)
{
   rtxPrintIndent ();
   printf ("%s = ", name);
   switch (*pvalue) {
      case 0: printf ("unavailable\n"); break;
      case 1: printf ("degSec-100-00\n"); break;
      case 2: printf ("degSec-010-00\n"); break;
      case 3: printf ("degSec-005-00\n"); break;
      case 4: printf ("degSec-001-00\n"); break;
      case 5: printf ("degSec-000-10\n"); break;
      case 6: printf ("degSec-000-05\n"); break;
      case 7: printf ("degSec-000-01\n"); break;
      default: printf ("???\n");
   }
}

void asn1Print_YawRate (const char* name, const YawRate* pvalue)
{
   rtxPrintIndent ();
   rtxPrintInteger (name, *pvalue);
}

void asn1Print_ZoneLength (const char* name, const ZoneLength* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_Zoom (const char* name, const Zoom* pvalue)
{
   rtxPrintIndent ();
   rtxPrintUnsigned (name, *pvalue);
}

void asn1Print_MessageFrame (const char* name, const MessageFrame* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_DSRCmsgID ("messageId", &pvalue->messageId);

   rtxPrintIndent ();
   rtxPrintHexStr ("value", pvalue->value.numocts,
   pvalue->value.data);

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_PositionalAccuracy 
   (const char* name, const PositionalAccuracy* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_SemiMajorAxisAccuracy ("semiMajor", &pvalue->semiMajor);

   asn1Print_SemiMinorAxisAccuracy ("semiMinor", &pvalue->semiMinor);

   asn1Print_SemiMajorAxisOrientation ("orientation", &pvalue->orientation);

   rtxPrintCloseBrace ();
}

void asn1Print_AccelerationSet4Way 
   (const char* name, const AccelerationSet4Way* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Acceleration ("long_", &pvalue->long_);

   asn1Print_Acceleration ("lat", &pvalue->lat);

   asn1Print_VerticalAcceleration ("vert", &pvalue->vert);

   asn1Print_YawRate ("yaw", &pvalue->yaw);

   rtxPrintCloseBrace ();
}

void asn1Print_BrakeSystemStatus 
   (const char* name, const BrakeSystemStatus* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_BrakeAppliedStatus ("wheelBrakes", &pvalue->wheelBrakes);

   asn1Print_TractionControlStatus ("traction", &pvalue->traction);

   asn1Print_AntiLockBrakeStatus ("abs_", &pvalue->abs_);

   asn1Print_StabilityControlStatus ("scs", &pvalue->scs);

   asn1Print_BrakeBoostApplied ("brakeBoost", &pvalue->brakeBoost);

   asn1Print_AuxiliaryBrakeStatus ("auxBrakes", &pvalue->auxBrakes);

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleSize (const char* name, const VehicleSize* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_VehicleWidth ("width", &pvalue->width);

   asn1Print_VehicleLength ("length", &pvalue->length);

   rtxPrintCloseBrace ();
}

void asn1Print_BSMcoreData (const char* name, const BSMcoreData* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_MsgCount ("msgCnt", &pvalue->msgCnt);

   asn1Print_TemporaryID ("id", &pvalue->id);

   asn1Print_DSecond ("secMark", &pvalue->secMark);

   asn1Print_Latitude ("lat", &pvalue->lat);

   asn1Print_Longitude ("long_", &pvalue->long_);

   asn1Print_Elevation ("elev", &pvalue->elev);

   asn1Print_PositionalAccuracy ("accuracy", &pvalue->accuracy);

   asn1Print_TransmissionState ("transmission", &pvalue->transmission);

   asn1Print_Speed ("speed", &pvalue->speed);

   asn1Print_Heading ("heading", &pvalue->heading);

   asn1Print_SteeringWheelAngle ("angle", &pvalue->angle);

   asn1Print_AccelerationSet4Way ("accelSet", &pvalue->accelSet);

   asn1Print_BrakeSystemStatus ("brakes", &pvalue->brakes);

   asn1Print_VehicleSize ("size", &pvalue->size);

   rtxPrintCloseBrace ();
}

void asn1Print_PartIIcontent (const char* name, const PartIIcontent* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_PartII_Id ("partII_Id", &pvalue->partII_Id);

   rtxPrintIndent ();
   rtxPrintHexStr ("partII_Value", pvalue->partII_Value.numocts,
   pvalue->partII_Value.data);

   rtxPrintCloseBrace ();
}

void asn1Print_BasicSafetyMessage_partII 
   (const char* name, const BasicSafetyMessage_partII* pvalue)
{
   PartIIcontent* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((PartIIcontent*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_PartIIcontent (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RegionalExtension 
   (const char* name, const RegionalExtension* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_RegionId ("regionId", &pvalue->regionId);

   rtxPrintIndent ();
   rtxPrintHexStr ("regExtValue", pvalue->regExtValue.numocts,
   pvalue->regExtValue.data);

   rtxPrintCloseBrace ();
}

void asn1Print_BasicSafetyMessage_regional 
   (const char* name, const BasicSafetyMessage_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_BasicSafetyMessage 
   (const char* name, const BasicSafetyMessage* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_BSMcoreData ("coreData", &pvalue->coreData);

   if (pvalue->m.partIIPresent) {
      asn1Print_BasicSafetyMessage_partII ("partII", &pvalue->partII);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_BasicSafetyMessage_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RequestedItemList 
   (const char* name, const RequestedItemList* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RequestedItem (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_CommonSafetyRequest_regional 
   (const char* name, const CommonSafetyRequest_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_CommonSafetyRequest 
   (const char* name, const CommonSafetyRequest* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   if (pvalue->m.msgCntPresent) {
      asn1Print_MsgCount ("msgCnt", &pvalue->msgCnt);
   }

   if (pvalue->m.idPresent) {
      asn1Print_TemporaryID ("id", &pvalue->id);
   }

   asn1Print_RequestedItemList ("requests", &pvalue->requests);

   if (pvalue->m.regionalPresent) {
      asn1Print_CommonSafetyRequest_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RoadSideAlert_description 
   (const char* name, const RoadSideAlert_description* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_ITIScodes (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_DDateTime (const char* name, const DDateTime* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.yearPresent) {
      asn1Print_DYear ("year", &pvalue->year);
   }

   if (pvalue->m.monthPresent) {
      asn1Print_DMonth ("month", &pvalue->month);
   }

   if (pvalue->m.dayPresent) {
      asn1Print_DDay ("day", &pvalue->day);
   }

   if (pvalue->m.hourPresent) {
      asn1Print_DHour ("hour", &pvalue->hour);
   }

   if (pvalue->m.minutePresent) {
      asn1Print_DMinute ("minute", &pvalue->minute);
   }

   if (pvalue->m.secondPresent) {
      asn1Print_DSecond ("second", &pvalue->second);
   }

   if (pvalue->m.offsetPresent) {
      asn1Print_DOffset ("offset", &pvalue->offset);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_TransmissionAndSpeed 
   (const char* name, const TransmissionAndSpeed* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_TransmissionState ("transmisson", &pvalue->transmisson);

   asn1Print_Velocity ("speed", &pvalue->speed);

   rtxPrintCloseBrace ();
}

void asn1Print_PositionConfidenceSet 
   (const char* name, const PositionConfidenceSet* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_PositionConfidence ("pos", &pvalue->pos);

   asn1Print_ElevationConfidence ("elevation", &pvalue->elevation);

   rtxPrintCloseBrace ();
}

void asn1Print_SpeedandHeadingandThrottleConfidence 
   (const char* name, const SpeedandHeadingandThrottleConfidence* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_HeadingConfidence ("heading", &pvalue->heading);

   asn1Print_SpeedConfidence ("speed", &pvalue->speed);

   asn1Print_ThrottleConfidence ("throttle", &pvalue->throttle);

   rtxPrintCloseBrace ();
}

void asn1Print_FullPositionVector 
   (const char* name, const FullPositionVector* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.utcTimePresent) {
      asn1Print_DDateTime ("utcTime", &pvalue->utcTime);
   }

   asn1Print_Longitude ("long_", &pvalue->long_);

   asn1Print_Latitude ("lat", &pvalue->lat);

   if (pvalue->m.elevationPresent) {
      asn1Print_Elevation ("elevation", &pvalue->elevation);
   }

   if (pvalue->m.headingPresent) {
      asn1Print_Heading ("heading", &pvalue->heading);
   }

   if (pvalue->m.speedPresent) {
      asn1Print_TransmissionAndSpeed ("speed", &pvalue->speed);
   }

   if (pvalue->m.posAccuracyPresent) {
      asn1Print_PositionalAccuracy ("posAccuracy", &pvalue->posAccuracy);
   }

   if (pvalue->m.timeConfidencePresent) {
      asn1Print_TimeConfidence ("timeConfidence", &pvalue->timeConfidence);
   }

   if (pvalue->m.posConfidencePresent) {
      asn1Print_PositionConfidenceSet ("posConfidence", &pvalue->posConfidence
         );
   }

   if (pvalue->m.speedConfidencePresent) {
      asn1Print_SpeedandHeadingandThrottleConfidence ("speedConfidence", &
         pvalue->speedConfidence);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RoadSideAlert_regional 
   (const char* name, const RoadSideAlert_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RoadSideAlert (const char* name, const RoadSideAlert* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_MsgCount ("msgCnt", &pvalue->msgCnt);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   asn1Print_ITIScodes ("typeEvent", &pvalue->typeEvent);

   if (pvalue->m.descriptionPresent) {
      asn1Print_RoadSideAlert_description ("description", &pvalue->description
         );
   }

   if (pvalue->m.priorityPresent) {
      asn1Print_Priority ("priority", &pvalue->priority);
   }

   if (pvalue->m.headingPresent) {
      asn1Print_HeadingSlice ("heading", &pvalue->heading);
   }

   if (pvalue->m.extentPresent) {
      asn1Print_Extent ("extent", &pvalue->extent);
   }

   if (pvalue->m.positionPresent) {
      asn1Print_FullPositionVector ("position", &pvalue->position);
   }

   if (pvalue->m.furtherInfoIDPresent) {
      asn1Print_FurtherInfoID ("furtherInfoID", &pvalue->furtherInfoID);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RoadSideAlert_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_PrivilegedEvents 
   (const char* name, const PrivilegedEvents* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_SSPindex ("sspRights", &pvalue->sspRights);

   asn1Print_PrivilegedEventFlags ("event", &pvalue->event);

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_EmergencyDetails 
   (const char* name, const EmergencyDetails* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_SSPindex ("sspRights", &pvalue->sspRights);

   asn1Print_SirenInUse ("sirenUse", &pvalue->sirenUse);

   asn1Print_LightbarInUse ("lightsUse", &pvalue->lightsUse);

   asn1Print_MultiVehicleResponse ("multi", &pvalue->multi);

   if (pvalue->m.eventsPresent) {
      asn1Print_PrivilegedEvents ("events", &pvalue->events);
   }

   if (pvalue->m.responseTypePresent) {
      asn1Print_ResponseType ("responseType", &pvalue->responseType);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_EmergencyVehicleAlert_regional 
   (const char* name, const EmergencyVehicleAlert_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_EmergencyVehicleAlert 
   (const char* name, const EmergencyVehicleAlert* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   if (pvalue->m.idPresent) {
      asn1Print_TemporaryID ("id", &pvalue->id);
   }

   asn1Print_RoadSideAlert ("rsaMsg", &pvalue->rsaMsg);

   if (pvalue->m.responseTypePresent) {
      asn1Print_ResponseType ("responseType", &pvalue->responseType);
   }

   if (pvalue->m.detailsPresent) {
      asn1Print_EmergencyDetails ("details", &pvalue->details);
   }

   if (pvalue->m.massPresent) {
      asn1Print_VehicleMass ("mass", &pvalue->mass);
   }

   if (pvalue->m.basicTypePresent) {
      asn1Print_VehicleType ("basicType", &pvalue->basicType);
   }

   if (pvalue->m.vehicleTypePresent) {
      asn1Print_VehicleGroupAffected ("vehicleType", &pvalue->vehicleType);
   }

   if (pvalue->m.responseEquipPresent) {
      asn1Print_IncidentResponseEquipment ("responseEquip", &pvalue->
         responseEquip);
   }

   if (pvalue->m.responderTypePresent) {
      asn1Print_ResponderGroupAffected ("responderType", &pvalue->responderType
         );
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_EmergencyVehicleAlert_regional ("regional", &pvalue->regional
         );
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_PathHistoryPoint 
   (const char* name, const PathHistoryPoint* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_OffsetLL_B18 ("latOffset", &pvalue->latOffset);

   asn1Print_OffsetLL_B18 ("lonOffset", &pvalue->lonOffset);

   asn1Print_VertOffset_B12 ("elevationOffset", &pvalue->elevationOffset);

   asn1Print_TimeOffset ("timeOffset", &pvalue->timeOffset);

   if (pvalue->m.speedPresent) {
      asn1Print_Speed ("speed", &pvalue->speed);
   }

   if (pvalue->m.posAccuracyPresent) {
      asn1Print_PositionalAccuracy ("posAccuracy", &pvalue->posAccuracy);
   }

   if (pvalue->m.headingPresent) {
      asn1Print_CoarseHeading ("heading", &pvalue->heading);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_PathHistoryPointList 
   (const char* name, const PathHistoryPointList* pvalue)
{
   PathHistoryPoint* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((PathHistoryPoint*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_PathHistoryPoint (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_PathHistory (const char* name, const PathHistory* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.initialPositionPresent) {
      asn1Print_FullPositionVector ("initialPosition", &pvalue->initialPosition
         );
   }

   if (pvalue->m.currGNSSstatusPresent) {
      asn1Print_GNSSstatus ("currGNSSstatus", &pvalue->currGNSSstatus);
   }

   asn1Print_PathHistoryPointList ("crumbData", &pvalue->crumbData);

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_PathPrediction (const char* name, const PathPrediction* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_RadiusOfCurvature ("radiusOfCurve", &pvalue->radiusOfCurve);

   asn1Print_Confidence ("confidence", &pvalue->confidence);

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_IntersectionReferenceID 
   (const char* name, const IntersectionReferenceID* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.regionPresent) {
      asn1Print_RoadRegulatorID ("region", &pvalue->region);
   }

   asn1Print_IntersectionID ("id", &pvalue->id);

   rtxPrintCloseBrace ();
}

void asn1Print_ApproachOrLane (const char* name, const ApproachOrLane* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* approach */
      case 1:
      {
         asn1Print_ApproachID ("approach", &pvalue->u.approach);
         break;
      }
      /* lane */
      case 2:
      {
         asn1Print_LaneID ("lane", &pvalue->u.lane);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_IntersectionCollision_regional 
   (const char* name, const IntersectionCollision_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_IntersectionCollision 
   (const char* name, const IntersectionCollision* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_MsgCount ("msgCnt", &pvalue->msgCnt);

   asn1Print_TemporaryID ("id", &pvalue->id);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   if (pvalue->m.partOnePresent) {
      asn1Print_BSMcoreData ("partOne", &pvalue->partOne);
   }

   if (pvalue->m.pathPresent) {
      asn1Print_PathHistory ("path", &pvalue->path);
   }

   if (pvalue->m.pathPredictionPresent) {
      asn1Print_PathPrediction ("pathPrediction", &pvalue->pathPrediction);
   }

   asn1Print_IntersectionReferenceID ("intersectionID", &pvalue->intersectionID
      );

   asn1Print_ApproachOrLane ("laneNumber", &pvalue->laneNumber);

   asn1Print_VehicleEventFlags ("eventFlag", &pvalue->eventFlag);

   if (pvalue->m.regionalPresent) {
      asn1Print_IntersectionCollision_regional ("regional", &pvalue->regional
         );
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_Position3D_regional 
   (const char* name, const Position3D_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_Position3D (const char* name, const Position3D* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Latitude ("lat", &pvalue->lat);

   asn1Print_Longitude ("long_", &pvalue->long_);

   if (pvalue->m.elevationPresent) {
      asn1Print_Elevation ("elevation", &pvalue->elevation);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_Position3D_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RegulatorySpeedLimit 
   (const char* name, const RegulatorySpeedLimit* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_SpeedLimitType ("type", &pvalue->type);

   asn1Print_Velocity ("speed", &pvalue->speed);

   rtxPrintCloseBrace ();
}

void asn1Print_SpeedLimitList (const char* name, const SpeedLimitList* pvalue)
{
   RegulatorySpeedLimit* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegulatorySpeedLimit*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegulatorySpeedLimit (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_LaneTypeAttributes 
   (const char* name, const LaneTypeAttributes* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* vehicle */
      case 1:
      {
         asn1Print_LaneAttributes_Vehicle ("vehicle", pvalue->u.vehicle);
         break;
      }
      /* crosswalk */
      case 2:
      {
         asn1Print_LaneAttributes_Crosswalk ("crosswalk", pvalue->u.crosswalk
            );
         break;
      }
      /* bikeLane */
      case 3:
      {
         asn1Print_LaneAttributes_Bike ("bikeLane", pvalue->u.bikeLane);
         break;
      }
      /* sidewalk */
      case 4:
      {
         asn1Print_LaneAttributes_Sidewalk ("sidewalk", pvalue->u.sidewalk);
         break;
      }
      /* median */
      case 5:
      {
         asn1Print_LaneAttributes_Barrier ("median", pvalue->u.median);
         break;
      }
      /* striping */
      case 6:
      {
         asn1Print_LaneAttributes_Striping ("striping", pvalue->u.striping);
         break;
      }
      /* trackedVehicle */
      case 7:
      {
         asn1Print_LaneAttributes_TrackedVehicle ("trackedVehicle", pvalue->
            u.trackedVehicle);
         break;
      }
      /* parking */
      case 8:
      {
         asn1Print_LaneAttributes_Parking ("parking", pvalue->u.parking);
         break;
      }
      /* extElem1 */
      case 9:
      {
         rtxPrintIndent ();
         rtxPrintHexStr ("extElem1", pvalue->u.extElem1->numocts,
         pvalue->u.extElem1->data);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_LaneAttributes (const char* name, const LaneAttributes* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_LaneDirection ("directionalUse", &pvalue->directionalUse);

   asn1Print_LaneSharing ("sharedWith", &pvalue->sharedWith);

   asn1Print_LaneTypeAttributes ("laneType", &pvalue->laneType);

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_Node_XY_20b (const char* name, const Node_XY_20b* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Offset_B10 ("x", &pvalue->x);

   asn1Print_Offset_B10 ("y", &pvalue->y);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_XY_22b (const char* name, const Node_XY_22b* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Offset_B11 ("x", &pvalue->x);

   asn1Print_Offset_B11 ("y", &pvalue->y);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_XY_24b (const char* name, const Node_XY_24b* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Offset_B12 ("x", &pvalue->x);

   asn1Print_Offset_B12 ("y", &pvalue->y);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_XY_26b (const char* name, const Node_XY_26b* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Offset_B13 ("x", &pvalue->x);

   asn1Print_Offset_B13 ("y", &pvalue->y);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_XY_28b (const char* name, const Node_XY_28b* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Offset_B14 ("x", &pvalue->x);

   asn1Print_Offset_B14 ("y", &pvalue->y);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_XY_32b (const char* name, const Node_XY_32b* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Offset_B16 ("x", &pvalue->x);

   asn1Print_Offset_B16 ("y", &pvalue->y);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_LLmD_64b (const char* name, const Node_LLmD_64b* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Longitude ("lon", &pvalue->lon);

   asn1Print_Latitude ("lat", &pvalue->lat);

   rtxPrintCloseBrace ();
}

void asn1Print_NodeOffsetPointXY 
   (const char* name, const NodeOffsetPointXY* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* node-XY1 */
      case 1:
      {
         asn1Print_Node_XY_20b ("node-XY1", pvalue->u.node_XY1);
         break;
      }
      /* node-XY2 */
      case 2:
      {
         asn1Print_Node_XY_22b ("node-XY2", pvalue->u.node_XY2);
         break;
      }
      /* node-XY3 */
      case 3:
      {
         asn1Print_Node_XY_24b ("node-XY3", pvalue->u.node_XY3);
         break;
      }
      /* node-XY4 */
      case 4:
      {
         asn1Print_Node_XY_26b ("node-XY4", pvalue->u.node_XY4);
         break;
      }
      /* node-XY5 */
      case 5:
      {
         asn1Print_Node_XY_28b ("node-XY5", pvalue->u.node_XY5);
         break;
      }
      /* node-XY6 */
      case 6:
      {
         asn1Print_Node_XY_32b ("node-XY6", pvalue->u.node_XY6);
         break;
      }
      /* node-LatLon */
      case 7:
      {
         asn1Print_Node_LLmD_64b ("node-LatLon", pvalue->u.node_LatLon);
         break;
      }
      /* regional */
      case 8:
      {
         asn1Print_RegionalExtension ("regional", pvalue->u.regional);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_NodeAttributeXYList 
   (const char* name, const NodeAttributeXYList* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_NodeAttributeXY (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_SegmentAttributeXYList 
   (const char* name, const SegmentAttributeXYList* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_SegmentAttributeXY (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_LaneDataAttribute_regional 
   (const char* name, const LaneDataAttribute_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_LaneDataAttribute 
   (const char* name, const LaneDataAttribute* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* pathEndPointAngle */
      case 1:
      {
         asn1Print_DeltaAngle ("pathEndPointAngle", &pvalue->
            u.pathEndPointAngle);
         break;
      }
      /* laneCrownPointCenter */
      case 2:
      {
         asn1Print_RoadwayCrownAngle ("laneCrownPointCenter", &pvalue->
            u.laneCrownPointCenter);
         break;
      }
      /* laneCrownPointLeft */
      case 3:
      {
         asn1Print_RoadwayCrownAngle ("laneCrownPointLeft", &pvalue->
            u.laneCrownPointLeft);
         break;
      }
      /* laneCrownPointRight */
      case 4:
      {
         asn1Print_RoadwayCrownAngle ("laneCrownPointRight", &pvalue->
            u.laneCrownPointRight);
         break;
      }
      /* laneAngle */
      case 5:
      {
         asn1Print_MergeDivergeNodeAngle ("laneAngle", &pvalue->u.laneAngle);
         break;
      }
      /* speedLimits */
      case 6:
      {
         asn1Print_SpeedLimitList ("speedLimits", pvalue->u.speedLimits);
         break;
      }
      /* regional */
      case 7:
      {
         asn1Print_LaneDataAttribute_regional ("regional", pvalue->u.regional
            );
         break;
      }
      /* extElem1 */
      case 8:
      {
         rtxPrintIndent ();
         rtxPrintHexStr ("extElem1", pvalue->u.extElem1->numocts,
         pvalue->u.extElem1->data);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_LaneDataAttributeList 
   (const char* name, const LaneDataAttributeList* pvalue)
{
   LaneDataAttribute* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((LaneDataAttribute*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_LaneDataAttribute (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_NodeAttributeSetXY_regional 
   (const char* name, const NodeAttributeSetXY_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_NodeAttributeSetXY 
   (const char* name, const NodeAttributeSetXY* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.localNodePresent) {
      asn1Print_NodeAttributeXYList ("localNode", &pvalue->localNode);
   }

   if (pvalue->m.disabledPresent) {
      asn1Print_SegmentAttributeXYList ("disabled", &pvalue->disabled);
   }

   if (pvalue->m.enabledPresent) {
      asn1Print_SegmentAttributeXYList ("enabled", &pvalue->enabled);
   }

   if (pvalue->m.dataPresent) {
      asn1Print_LaneDataAttributeList ("data", &pvalue->data);
   }

   if (pvalue->m.dWidthPresent) {
      asn1Print_Offset_B10 ("dWidth", &pvalue->dWidth);
   }

   if (pvalue->m.dElevationPresent) {
      asn1Print_Offset_B10 ("dElevation", &pvalue->dElevation);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_NodeAttributeSetXY_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_NodeXY (const char* name, const NodeXY* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_NodeOffsetPointXY ("delta", &pvalue->delta);

   if (pvalue->m.attributesPresent) {
      asn1Print_NodeAttributeSetXY ("attributes", &pvalue->attributes);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_NodeSetXY (const char* name, const NodeSetXY* pvalue)
{
   NodeXY* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((NodeXY*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_NodeXY (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_ComputedLane_offsetXaxis 
   (const char* name, const ComputedLane_offsetXaxis* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* small */
      case 1:
      {
         asn1Print_DrivenLineOffsetSm ("small", &pvalue->u.small_);
         break;
      }
      /* large */
      case 2:
      {
         asn1Print_DrivenLineOffsetLg ("large", &pvalue->u.large_);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_ComputedLane_offsetYaxis 
   (const char* name, const ComputedLane_offsetYaxis* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* small */
      case 1:
      {
         asn1Print_DrivenLineOffsetSm ("small", &pvalue->u.small_);
         break;
      }
      /* large */
      case 2:
      {
         asn1Print_DrivenLineOffsetLg ("large", &pvalue->u.large_);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_ComputedLane_regional 
   (const char* name, const ComputedLane_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_ComputedLane (const char* name, const ComputedLane* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_LaneID ("referenceLaneId", &pvalue->referenceLaneId);

   asn1Print_ComputedLane_offsetXaxis ("offsetXaxis", &pvalue->offsetXaxis);

   asn1Print_ComputedLane_offsetYaxis ("offsetYaxis", &pvalue->offsetYaxis);

   if (pvalue->m.rotateXYPresent) {
      asn1Print_Angle ("rotateXY", &pvalue->rotateXY);
   }

   if (pvalue->m.scaleXaxisPresent) {
      asn1Print_Scale_B12 ("scaleXaxis", &pvalue->scaleXaxis);
   }

   if (pvalue->m.scaleYaxisPresent) {
      asn1Print_Scale_B12 ("scaleYaxis", &pvalue->scaleYaxis);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_ComputedLane_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_NodeListXY (const char* name, const NodeListXY* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* nodes */
      case 1:
      {
         asn1Print_NodeSetXY ("nodes", pvalue->u.nodes);
         break;
      }
      /* computed */
      case 2:
      {
         asn1Print_ComputedLane ("computed", pvalue->u.computed);
         break;
      }
      /* extElem1 */
      case 3:
      {
         rtxPrintIndent ();
         rtxPrintHexStr ("extElem1", pvalue->u.extElem1->numocts,
         pvalue->u.extElem1->data);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_ConnectingLane (const char* name, const ConnectingLane* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_LaneID ("lane", &pvalue->lane);

   if (pvalue->m.maneuverPresent) {
      asn1Print_AllowedManeuvers ("maneuver", &pvalue->maneuver);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_Connection (const char* name, const Connection* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_ConnectingLane ("connectingLane", &pvalue->connectingLane);

   if (pvalue->m.remoteIntersectionPresent) {
      asn1Print_IntersectionReferenceID ("remoteIntersection", &pvalue->
         remoteIntersection);
   }

   if (pvalue->m.signalGroupPresent) {
      asn1Print_SignalGroupID ("signalGroup", &pvalue->signalGroup);
   }

   if (pvalue->m.userClassPresent) {
      asn1Print_RestrictionClassID ("userClass", &pvalue->userClass);
   }

   if (pvalue->m.connectionIDPresent) {
      asn1Print_LaneConnectionID ("connectionID", &pvalue->connectionID);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_ConnectsToList (const char* name, const ConnectsToList* pvalue)
{
   Connection* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((Connection*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_Connection (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_OverlayLaneList 
   (const char* name, const OverlayLaneList* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_LaneID (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_GenericLane_regional 
   (const char* name, const GenericLane_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_GenericLane (const char* name, const GenericLane* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_LaneID ("laneID", &pvalue->laneID);

   if (pvalue->m.namePresent) {
      asn1Print_DescriptiveName ("name", pvalue->name);
   }

   if (pvalue->m.ingressApproachPresent) {
      asn1Print_ApproachID ("ingressApproach", &pvalue->ingressApproach);
   }

   if (pvalue->m.egressApproachPresent) {
      asn1Print_ApproachID ("egressApproach", &pvalue->egressApproach);
   }

   asn1Print_LaneAttributes ("laneAttributes", &pvalue->laneAttributes);

   if (pvalue->m.maneuversPresent) {
      asn1Print_AllowedManeuvers ("maneuvers", &pvalue->maneuvers);
   }

   asn1Print_NodeListXY ("nodeList", &pvalue->nodeList);

   if (pvalue->m.connectsToPresent) {
      asn1Print_ConnectsToList ("connectsTo", &pvalue->connectsTo);
   }

   if (pvalue->m.overlaysPresent) {
      asn1Print_OverlayLaneList ("overlays", &pvalue->overlays);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_GenericLane_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_LaneList (const char* name, const LaneList* pvalue)
{
   GenericLane* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((GenericLane*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_GenericLane (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SignalControlZone 
   (const char* name, const SignalControlZone* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_RegionalExtension ("zone", &pvalue->zone);

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_PreemptPriorityList 
   (const char* name, const PreemptPriorityList* pvalue)
{
   SignalControlZone* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((SignalControlZone*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_SignalControlZone (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_IntersectionGeometry_regional 
   (const char* name, const IntersectionGeometry_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_IntersectionGeometry 
   (const char* name, const IntersectionGeometry* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.namePresent) {
      asn1Print_DescriptiveName ("name", pvalue->name);
   }

   asn1Print_IntersectionReferenceID ("id", &pvalue->id);

   asn1Print_MsgCount ("revision", &pvalue->revision);

   asn1Print_Position3D ("refPoint", &pvalue->refPoint);

   if (pvalue->m.laneWidthPresent) {
      asn1Print_LaneWidth ("laneWidth", &pvalue->laneWidth);
   }

   if (pvalue->m.speedLimitsPresent) {
      asn1Print_SpeedLimitList ("speedLimits", &pvalue->speedLimits);
   }

   asn1Print_LaneList ("laneSet", &pvalue->laneSet);

   if (pvalue->m.preemptPriorityDataPresent) {
      asn1Print_PreemptPriorityList ("preemptPriorityData", &pvalue->
         preemptPriorityData);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_IntersectionGeometry_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_IntersectionGeometryList 
   (const char* name, const IntersectionGeometryList* pvalue)
{
   IntersectionGeometry* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((IntersectionGeometry*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_IntersectionGeometry (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RoadSegmentReferenceID 
   (const char* name, const RoadSegmentReferenceID* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.regionPresent) {
      asn1Print_RoadRegulatorID ("region", &pvalue->region);
   }

   asn1Print_RoadSegmentID ("id", &pvalue->id);

   rtxPrintCloseBrace ();
}

void asn1Print_RoadLaneSetList 
   (const char* name, const RoadLaneSetList* pvalue)
{
   GenericLane* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((GenericLane*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_GenericLane (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RoadSegment_regional 
   (const char* name, const RoadSegment_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RoadSegment (const char* name, const RoadSegment* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.namePresent) {
      asn1Print_DescriptiveName ("name", pvalue->name);
   }

   asn1Print_RoadSegmentReferenceID ("id", &pvalue->id);

   asn1Print_MsgCount ("revision", &pvalue->revision);

   asn1Print_Position3D ("refPoint", &pvalue->refPoint);

   if (pvalue->m.laneWidthPresent) {
      asn1Print_LaneWidth ("laneWidth", &pvalue->laneWidth);
   }

   if (pvalue->m.speedLimitsPresent) {
      asn1Print_SpeedLimitList ("speedLimits", &pvalue->speedLimits);
   }

   asn1Print_RoadLaneSetList ("roadLaneSet", &pvalue->roadLaneSet);

   if (pvalue->m.regionalPresent) {
      asn1Print_RoadSegment_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RoadSegmentList 
   (const char* name, const RoadSegmentList* pvalue)
{
   RoadSegment* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RoadSegment*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RoadSegment (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_DataParameters (const char* name, const DataParameters* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.processMethodPresent) {
      rtxPrintIndent ();
      rtxPrintCharStr ("processMethod", pvalue->processMethod);
   }

   if (pvalue->m.processAgencyPresent) {
      rtxPrintIndent ();
      rtxPrintCharStr ("processAgency", pvalue->processAgency);
   }

   if (pvalue->m.lastCheckedDatePresent) {
      rtxPrintIndent ();
      rtxPrintCharStr ("lastCheckedDate", pvalue->lastCheckedDate);
   }

   if (pvalue->m.geoidUsedPresent) {
      rtxPrintIndent ();
      rtxPrintCharStr ("geoidUsed", pvalue->geoidUsed);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RestrictionUserType_regional 
   (const char* name, const RestrictionUserType_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RestrictionUserType 
   (const char* name, const RestrictionUserType* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* basicType */
      case 1:
      {
         asn1Print_RestrictionAppliesTo ("basicType", &pvalue->u.basicType);
         break;
      }
      /* regional */
      case 2:
      {
         asn1Print_RestrictionUserType_regional ("regional", pvalue->u.regional
            );
         break;
      }
      /* extElem1 */
      case 3:
      {
         rtxPrintIndent ();
         rtxPrintHexStr ("extElem1", pvalue->u.extElem1->numocts,
         pvalue->u.extElem1->data);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_RestrictionUserTypeList 
   (const char* name, const RestrictionUserTypeList* pvalue)
{
   RestrictionUserType* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RestrictionUserType*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RestrictionUserType (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RestrictionClassAssignment 
   (const char* name, const RestrictionClassAssignment* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_RestrictionClassID ("id", &pvalue->id);

   asn1Print_RestrictionUserTypeList ("users", &pvalue->users);

   rtxPrintCloseBrace ();
}

void asn1Print_RestrictionClassList 
   (const char* name, const RestrictionClassList* pvalue)
{
   RestrictionClassAssignment* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RestrictionClassAssignment*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RestrictionClassAssignment (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_MapData_regional 
   (const char* name, const MapData_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_MapData (const char* name, const MapData* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   asn1Print_MsgCount ("msgIssueRevision", &pvalue->msgIssueRevision);

   if (pvalue->m.layerTypePresent) {
      asn1Print_LayerType ("layerType", &pvalue->layerType);
   }

   if (pvalue->m.layerIDPresent) {
      asn1Print_LayerID ("layerID", &pvalue->layerID);
   }

   if (pvalue->m.intersectionsPresent) {
      asn1Print_IntersectionGeometryList ("intersections", &pvalue->
         intersections);
   }

   if (pvalue->m.roadSegmentsPresent) {
      asn1Print_RoadSegmentList ("roadSegments", &pvalue->roadSegments);
   }

   if (pvalue->m.dataParametersPresent) {
      asn1Print_DataParameters ("dataParameters", &pvalue->dataParameters);
   }

   if (pvalue->m.restrictionListPresent) {
      asn1Print_RestrictionClassList ("restrictionList", &pvalue->
         restrictionList);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_MapData_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_NMEAcorrections_regional 
   (const char* name, const NMEAcorrections_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_NMEAcorrections 
   (const char* name, const NMEAcorrections* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   if (pvalue->m.revPresent) {
      asn1Print_NMEA_Revision ("rev", &pvalue->rev);
   }

   if (pvalue->m.msgPresent) {
      asn1Print_NMEA_MsgType ("msg", &pvalue->msg);
   }

   if (pvalue->m.wdCountPresent) {
      asn1Print_ObjectCount ("wdCount", &pvalue->wdCount);
   }

   asn1Print_NMEA_Payload ("payload", &pvalue->payload);

   if (pvalue->m.regionalPresent) {
      asn1Print_NMEAcorrections_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_PropelledInformation 
   (const char* name, const PropelledInformation* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* human */
      case 1:
      {
         asn1Print_HumanPropelledType ("human", &pvalue->u.human);
         break;
      }
      /* animal */
      case 2:
      {
         asn1Print_AnimalPropelledType ("animal", &pvalue->u.animal);
         break;
      }
      /* motor */
      case 3:
      {
         asn1Print_MotorizedPropelledType ("motor", &pvalue->u.motor);
         break;
      }
      /* extElem1 */
      case 4:
      {
         rtxPrintIndent ();
         rtxPrintHexStr ("extElem1", pvalue->u.extElem1->numocts,
         pvalue->u.extElem1->data);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_PersonalSafetyMessage_regional 
   (const char* name, const PersonalSafetyMessage_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_PersonalSafetyMessage 
   (const char* name, const PersonalSafetyMessage* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_PersonalDeviceUserType ("basicType", &pvalue->basicType);

   asn1Print_DSecond ("secMark", &pvalue->secMark);

   asn1Print_MsgCount ("msgCnt", &pvalue->msgCnt);

   asn1Print_TemporaryID ("id", &pvalue->id);

   asn1Print_Position3D ("position", &pvalue->position);

   asn1Print_PositionalAccuracy ("accuracy", &pvalue->accuracy);

   asn1Print_Velocity ("speed", &pvalue->speed);

   asn1Print_Heading ("heading", &pvalue->heading);

   if (pvalue->m.accelSetPresent) {
      asn1Print_AccelerationSet4Way ("accelSet", &pvalue->accelSet);
   }

   if (pvalue->m.pathHistoryPresent) {
      asn1Print_PathHistory ("pathHistory", &pvalue->pathHistory);
   }

   if (pvalue->m.pathPredictionPresent) {
      asn1Print_PathPrediction ("pathPrediction", &pvalue->pathPrediction);
   }

   if (pvalue->m.propulsionPresent) {
      asn1Print_PropelledInformation ("propulsion", &pvalue->propulsion);
   }

   if (pvalue->m.useStatePresent) {
      asn1Print_PersonalDeviceUsageState ("useState", &pvalue->useState);
   }

   if (pvalue->m.crossRequestPresent) {
      asn1Print_PersonalCrossingRequest ("crossRequest", &pvalue->crossRequest
         );
   }

   if (pvalue->m.crossStatePresent) {
      asn1Print_PersonalCrossingInProgress ("crossState", &pvalue->crossState
         );
   }

   if (pvalue->m.clusterSizePresent) {
      asn1Print_NumberOfParticipantsInCluster ("clusterSize", &pvalue->
         clusterSize);
   }

   if (pvalue->m.clusterRadiusPresent) {
      asn1Print_PersonalClusterRadius ("clusterRadius", &pvalue->clusterRadius
         );
   }

   if (pvalue->m.eventResponderTypePresent) {
      asn1Print_PublicSafetyEventResponderWorkerType ("eventResponderType", &
         pvalue->eventResponderType);
   }

   if (pvalue->m.activityTypePresent) {
      asn1Print_PublicSafetyAndRoadWorkerActivity ("activityType", &pvalue->
         activityType);
   }

   if (pvalue->m.activitySubTypePresent) {
      asn1Print_PublicSafetyDirectingTrafficSubType ("activitySubType", &pvalue
         ->activitySubType);
   }

   if (pvalue->m.assistTypePresent) {
      asn1Print_PersonalAssistive ("assistType", &pvalue->assistType);
   }

   if (pvalue->m.sizingPresent) {
      asn1Print_UserSizeAndBehaviour ("sizing", &pvalue->sizing);
   }

   if (pvalue->m.attachmentPresent) {
      asn1Print_Attachment ("attachment", &pvalue->attachment);
   }

   if (pvalue->m.attachmentRadiusPresent) {
      asn1Print_AttachmentRadius ("attachmentRadius", &pvalue->attachmentRadius
         );
   }

   if (pvalue->m.animalTypePresent) {
      asn1Print_AnimalType ("animalType", &pvalue->animalType);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_PersonalSafetyMessage_regional ("regional", &pvalue->regional
         );
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_Sample (const char* name, const Sample* pvalue)
{
   rtxPrintOpenBrace (name);

   rtxPrintIndent ();
   rtxPrintUnsigned ("sampleStart", pvalue->sampleStart);

   rtxPrintIndent ();
   rtxPrintUnsigned ("sampleEnd", pvalue->sampleEnd);

   rtxPrintCloseBrace ();
}

void asn1Print_ProbeDataManagement_term 
   (const char* name, const ProbeDataManagement_term* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* termtime */
      case 1:
      {
         asn1Print_TermTime ("termtime", &pvalue->u.termtime);
         break;
      }
      /* termDistance */
      case 2:
      {
         asn1Print_TermDistance ("termDistance", &pvalue->u.termDistance);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_SnapshotTime (const char* name, const SnapshotTime* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_GrossSpeed ("speed1", &pvalue->speed1);

   asn1Print_SecondOfTime ("time1", &pvalue->time1);

   asn1Print_GrossSpeed ("speed2", &pvalue->speed2);

   asn1Print_SecondOfTime ("time2", &pvalue->time2);

   rtxPrintCloseBrace ();
}

void asn1Print_SnapshotDistance 
   (const char* name, const SnapshotDistance* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_GrossDistance ("distance1", &pvalue->distance1);

   asn1Print_GrossSpeed ("speed1", &pvalue->speed1);

   asn1Print_GrossDistance ("distance2", &pvalue->distance2);

   asn1Print_GrossSpeed ("speed2", &pvalue->speed2);

   rtxPrintCloseBrace ();
}

void asn1Print_ProbeDataManagement_snapshot 
   (const char* name, const ProbeDataManagement_snapshot* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* snapshotTime */
      case 1:
      {
         asn1Print_SnapshotTime ("snapshotTime", pvalue->u.snapshotTime);
         break;
      }
      /* snapshotDistance */
      case 2:
      {
         asn1Print_SnapshotDistance ("snapshotDistance", pvalue->
            u.snapshotDistance);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleStatusRequest 
   (const char* name, const VehicleStatusRequest* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_VehicleStatusDeviceTypeTag ("dataType", &pvalue->dataType);

   if (pvalue->m.subTypePresent) {
      rtxPrintIndent ();
      rtxPrintUnsigned ("subType", pvalue->subType);
   }

   if (pvalue->m.sendOnLessThenValuePresent) {
      rtxPrintIndent ();
      rtxPrintInteger ("sendOnLessThenValue", pvalue->sendOnLessThenValue);
   }

   if (pvalue->m.sendOnMoreThenValuePresent) {
      rtxPrintIndent ();
      rtxPrintInteger ("sendOnMoreThenValue", pvalue->sendOnMoreThenValue);
   }

   if (pvalue->m.sendAllPresent) {
      rtxPrintIndent ();
      rtxPrintBoolean ("sendAll", pvalue->sendAll);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleStatusRequestList 
   (const char* name, const VehicleStatusRequestList* pvalue)
{
   VehicleStatusRequest* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((VehicleStatusRequest*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_VehicleStatusRequest (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_ProbeDataManagement_regional 
   (const char* name, const ProbeDataManagement_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_ProbeDataManagement 
   (const char* name, const ProbeDataManagement* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   asn1Print_Sample ("sample", &pvalue->sample);

   asn1Print_HeadingSlice ("directions", &pvalue->directions);

   asn1Print_ProbeDataManagement_term ("term", &pvalue->term);

   asn1Print_ProbeDataManagement_snapshot ("snapshot", &pvalue->snapshot);

   asn1Print_SecondOfTime ("txInterval", &pvalue->txInterval);

   if (pvalue->m.dataElementsPresent) {
      asn1Print_VehicleStatusRequestList ("dataElements", &pvalue->dataElements
         );
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_ProbeDataManagement_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleID (const char* name, const VehicleID* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* entityID */
      case 1:
      {
         asn1Print_TemporaryID ("entityID", pvalue->u.entityID);
         break;
      }
      /* stationID */
      case 2:
      {
         asn1Print_StationID ("stationID", &pvalue->u.stationID);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleIdent_vehicleClass 
   (const char* name, const VehicleIdent_vehicleClass* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* vGroup */
      case 1:
      {
         asn1Print_VehicleGroupAffected ("vGroup", &pvalue->u.vGroup);
         break;
      }
      /* rGroup */
      case 2:
      {
         asn1Print_ResponderGroupAffected ("rGroup", &pvalue->u.rGroup);
         break;
      }
      /* rEquip */
      case 3:
      {
         asn1Print_IncidentResponseEquipment ("rEquip", &pvalue->u.rEquip);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleIdent (const char* name, const VehicleIdent* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.namePresent) {
      asn1Print_DescriptiveName ("name", pvalue->name);
   }

   if (pvalue->m.vinPresent) {
      asn1Print_VINstring ("vin", &pvalue->vin);
   }

   if (pvalue->m.ownerCodePresent) {
      rtxPrintIndent ();
      rtxPrintCharStr ("ownerCode", pvalue->ownerCode);
   }

   if (pvalue->m.idPresent) {
      asn1Print_VehicleID ("id", &pvalue->id);
   }

   if (pvalue->m.vehicleTypePresent) {
      asn1Print_VehicleType ("vehicleType", &pvalue->vehicleType);
   }

   if (pvalue->m.vehicleClassPresent) {
      asn1Print_VehicleIdent_vehicleClass ("vehicleClass", &pvalue->
         vehicleClass);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleClassification_regional 
   (const char* name, const VehicleClassification_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_VehicleClassification 
   (const char* name, const VehicleClassification* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.keyTypePresent) {
      asn1Print_BasicVehicleClass ("keyType", &pvalue->keyType);
   }

   if (pvalue->m.rolePresent) {
      asn1Print_BasicVehicleRole ("role", &pvalue->role);
   }

   if (pvalue->m.iso3883Present) {
      asn1Print_Iso3833VehicleType ("iso3883", &pvalue->iso3883);
   }

   if (pvalue->m.hpmsTypePresent) {
      asn1Print_VehicleType ("hpmsType", &pvalue->hpmsType);
   }

   if (pvalue->m.vehicleTypePresent) {
      asn1Print_VehicleGroupAffected ("vehicleType", &pvalue->vehicleType);
   }

   if (pvalue->m.responseEquipPresent) {
      asn1Print_IncidentResponseEquipment ("responseEquip", &pvalue->
         responseEquip);
   }

   if (pvalue->m.responderTypePresent) {
      asn1Print_ResponderGroupAffected ("responderType", &pvalue->responderType
         );
   }

   if (pvalue->m.fuelTypePresent) {
      asn1Print_FuelType ("fuelType", &pvalue->fuelType);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_VehicleClassification_regional ("regional", &pvalue->regional
         );
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleSafetyExtensions 
   (const char* name, const VehicleSafetyExtensions* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.eventsPresent) {
      asn1Print_VehicleEventFlags ("events", &pvalue->events);
   }

   if (pvalue->m.pathHistoryPresent) {
      asn1Print_PathHistory ("pathHistory", &pvalue->pathHistory);
   }

   if (pvalue->m.pathPredictionPresent) {
      asn1Print_PathPrediction ("pathPrediction", &pvalue->pathPrediction);
   }

   if (pvalue->m.lightsPresent) {
      asn1Print_ExteriorLights ("lights", &pvalue->lights);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_WiperSet (const char* name, const WiperSet* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_WiperStatus ("statusFront", &pvalue->statusFront);

   asn1Print_WiperRate ("rateFront", &pvalue->rateFront);

   if (pvalue->m.statusRearPresent) {
      asn1Print_WiperStatus ("statusRear", &pvalue->statusRear);
   }

   if (pvalue->m.rateRearPresent) {
      asn1Print_WiperRate ("rateRear", &pvalue->rateRear);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleStatus_steering 
   (const char* name, const VehicleStatus_steering* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_SteeringWheelAngle ("angle", &pvalue->angle);

   if (pvalue->m.confidencePresent) {
      asn1Print_SteeringWheelAngleConfidence ("confidence", &pvalue->confidence
         );
   }

   if (pvalue->m.ratePresent) {
      asn1Print_SteeringWheelAngleRateOfChange ("rate", &pvalue->rate);
   }

   if (pvalue->m.wheelsPresent) {
      asn1Print_DrivingWheelAngle ("wheels", &pvalue->wheels);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_AccelSteerYawRateConfidence 
   (const char* name, const AccelSteerYawRateConfidence* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_YawRateConfidence ("yawRate", &pvalue->yawRate);

   asn1Print_AccelerationConfidence ("acceleration", &pvalue->acceleration);

   asn1Print_SteeringWheelAngleConfidence ("steeringWheelAngle", &pvalue->
      steeringWheelAngle);

   rtxPrintCloseBrace ();
}

void asn1Print_ConfidenceSet (const char* name, const ConfidenceSet* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.accelConfidencePresent) {
      asn1Print_AccelSteerYawRateConfidence ("accelConfidence", &pvalue->
         accelConfidence);
   }

   if (pvalue->m.speedConfidencePresent) {
      asn1Print_SpeedandHeadingandThrottleConfidence ("speedConfidence", &
         pvalue->speedConfidence);
   }

   if (pvalue->m.timeConfidencePresent) {
      asn1Print_TimeConfidence ("timeConfidence", &pvalue->timeConfidence);
   }

   if (pvalue->m.posConfidencePresent) {
      asn1Print_PositionConfidenceSet ("posConfidence", &pvalue->posConfidence
         );
   }

   if (pvalue->m.steerConfidencePresent) {
      asn1Print_SteeringWheelAngleConfidence ("steerConfidence", &pvalue->
         steerConfidence);
   }

   if (pvalue->m.headingConfidencePresent) {
      asn1Print_HeadingConfidence ("headingConfidence", &pvalue->
         headingConfidence);
   }

   if (pvalue->m.throttleConfidencePresent) {
      asn1Print_ThrottleConfidence ("throttleConfidence", &pvalue->
         throttleConfidence);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleStatus_accelSets 
   (const char* name, const VehicleStatus_accelSets* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.accel4wayPresent) {
      asn1Print_AccelerationSet4Way ("accel4way", &pvalue->accel4way);
   }

   if (pvalue->m.vertAccelThresPresent) {
      asn1Print_VerticalAccelerationThreshold ("vertAccelThres", &pvalue->
         vertAccelThres);
   }

   if (pvalue->m.yawRateConPresent) {
      asn1Print_YawRateConfidence ("yawRateCon", &pvalue->yawRateCon);
   }

   if (pvalue->m.hozAccelConPresent) {
      asn1Print_AccelerationConfidence ("hozAccelCon", &pvalue->hozAccelCon);
   }

   if (pvalue->m.confidenceSetPresent) {
      asn1Print_ConfidenceSet ("confidenceSet", &pvalue->confidenceSet);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleStatus_object 
   (const char* name, const VehicleStatus_object* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_ObstacleDistance ("obDist", &pvalue->obDist);

   asn1Print_Angle ("obDirect", &pvalue->obDirect);

   asn1Print_DDateTime ("dateTime", &pvalue->dateTime);

   rtxPrintCloseBrace ();
}

void asn1Print_BumperHeights (const char* name, const BumperHeights* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_BumperHeight ("front", &pvalue->front);

   asn1Print_BumperHeight ("rear", &pvalue->rear);

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleStatus_vehicleData 
   (const char* name, const VehicleStatus_vehicleData* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_VehicleHeight ("height", &pvalue->height);

   asn1Print_BumperHeights ("bumpers", &pvalue->bumpers);

   asn1Print_VehicleMass ("mass", &pvalue->mass);

   asn1Print_TrailerWeight ("trailerWeight", &pvalue->trailerWeight);

   asn1Print_VehicleType ("type", &pvalue->type);

   rtxPrintCloseBrace ();
}

void asn1Print_TireData (const char* name, const TireData* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.locationPresent) {
      asn1Print_TireLocation ("location", &pvalue->location);
   }

   if (pvalue->m.pressurePresent) {
      asn1Print_TirePressure ("pressure", &pvalue->pressure);
   }

   if (pvalue->m.tempPresent) {
      asn1Print_TireTemp ("temp", &pvalue->temp);
   }

   if (pvalue->m.wheelSensorStatusPresent) {
      asn1Print_WheelSensorStatus ("wheelSensorStatus", &pvalue->
         wheelSensorStatus);
   }

   if (pvalue->m.wheelEndElectFaultPresent) {
      asn1Print_WheelEndElectFault ("wheelEndElectFault", &pvalue->
         wheelEndElectFault);
   }

   if (pvalue->m.leakageRatePresent) {
      asn1Print_TireLeakageRate ("leakageRate", &pvalue->leakageRate);
   }

   if (pvalue->m.detectionPresent) {
      asn1Print_TirePressureThresholdDetection ("detection", &pvalue->detection
         );
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TireDataList (const char* name, const TireDataList* pvalue)
{
   TireData* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((TireData*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_TireData (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_AxleWeightSet (const char* name, const AxleWeightSet* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.locationPresent) {
      asn1Print_AxleLocation ("location", &pvalue->location);
   }

   if (pvalue->m.weightPresent) {
      asn1Print_AxleWeight ("weight", &pvalue->weight);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_AxleWeightList (const char* name, const AxleWeightList* pvalue)
{
   AxleWeightSet* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((AxleWeightSet*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_AxleWeightSet (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_J1939data (const char* name, const J1939data* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.tiresPresent) {
      asn1Print_TireDataList ("tires", &pvalue->tires);
   }

   if (pvalue->m.axlesPresent) {
      asn1Print_AxleWeightList ("axles", &pvalue->axles);
   }

   if (pvalue->m.trailerWeightPresent) {
      asn1Print_TrailerWeight ("trailerWeight", &pvalue->trailerWeight);
   }

   if (pvalue->m.cargoWeightPresent) {
      asn1Print_CargoWeight ("cargoWeight", &pvalue->cargoWeight);
   }

   if (pvalue->m.steeringAxleTemperaturePresent) {
      asn1Print_SteeringAxleTemperature ("steeringAxleTemperature", &pvalue->
         steeringAxleTemperature);
   }

   if (pvalue->m.driveAxleLocationPresent) {
      asn1Print_DriveAxleLocation ("driveAxleLocation", &pvalue->
         driveAxleLocation);
   }

   if (pvalue->m.driveAxleLiftAirPressurePresent) {
      asn1Print_DriveAxleLiftAirPressure ("driveAxleLiftAirPressure", &pvalue->
         driveAxleLiftAirPressure);
   }

   if (pvalue->m.driveAxleTemperaturePresent) {
      asn1Print_DriveAxleTemperature ("driveAxleTemperature", &pvalue->
         driveAxleTemperature);
   }

   if (pvalue->m.driveAxleLubePressurePresent) {
      asn1Print_DriveAxleLubePressure ("driveAxleLubePressure", &pvalue->
         driveAxleLubePressure);
   }

   if (pvalue->m.steeringAxleLubePressurePresent) {
      asn1Print_SteeringAxleLubePressure ("steeringAxleLubePressure", &pvalue->
         steeringAxleLubePressure);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleStatus_weatherReport 
   (const char* name, const VehicleStatus_weatherReport* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_EssPrecipYesNo ("isRaining", &pvalue->isRaining);

   if (pvalue->m.rainRatePresent) {
      asn1Print_EssPrecipRate ("rainRate", &pvalue->rainRate);
   }

   if (pvalue->m.precipSituationPresent) {
      asn1Print_EssPrecipSituation ("precipSituation", &pvalue->precipSituation
         );
   }

   if (pvalue->m.solarRadiationPresent) {
      asn1Print_EssSolarRadiation ("solarRadiation", &pvalue->solarRadiation);
   }

   if (pvalue->m.frictionPresent) {
      asn1Print_EssMobileFriction ("friction", &pvalue->friction);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleStatus (const char* name, const VehicleStatus* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.lightsPresent) {
      asn1Print_ExteriorLights ("lights", &pvalue->lights);
   }

   if (pvalue->m.lightBarPresent) {
      asn1Print_LightbarInUse ("lightBar", &pvalue->lightBar);
   }

   if (pvalue->m.wipersPresent) {
      asn1Print_WiperSet ("wipers", &pvalue->wipers);
   }

   if (pvalue->m.brakeStatusPresent) {
      asn1Print_BrakeSystemStatus ("brakeStatus", &pvalue->brakeStatus);
   }

   if (pvalue->m.brakePressurePresent) {
      asn1Print_BrakeAppliedPressure ("brakePressure", &pvalue->brakePressure
         );
   }

   if (pvalue->m.roadFrictionPresent) {
      asn1Print_CoefficientOfFriction ("roadFriction", &pvalue->roadFriction);
   }

   if (pvalue->m.sunDataPresent) {
      asn1Print_SunSensor ("sunData", &pvalue->sunData);
   }

   if (pvalue->m.rainDataPresent) {
      asn1Print_RainSensor ("rainData", &pvalue->rainData);
   }

   if (pvalue->m.airTempPresent) {
      asn1Print_AmbientAirTemperature ("airTemp", &pvalue->airTemp);
   }

   if (pvalue->m.airPresPresent) {
      asn1Print_AmbientAirPressure ("airPres", &pvalue->airPres);
   }

   if (pvalue->m.steeringPresent) {
      asn1Print_VehicleStatus_steering ("steering", &pvalue->steering);
   }

   if (pvalue->m.accelSetsPresent) {
      asn1Print_VehicleStatus_accelSets ("accelSets", &pvalue->accelSets);
   }

   if (pvalue->m.objectPresent) {
      asn1Print_VehicleStatus_object ("object", &pvalue->object);
   }

   if (pvalue->m.fullPosPresent) {
      asn1Print_FullPositionVector ("fullPos", &pvalue->fullPos);
   }

   if (pvalue->m.throttlePosPresent) {
      asn1Print_ThrottlePosition ("throttlePos", &pvalue->throttlePos);
   }

   if (pvalue->m.speedHeadCPresent) {
      asn1Print_SpeedandHeadingandThrottleConfidence ("speedHeadC", &pvalue->
         speedHeadC);
   }

   if (pvalue->m.speedCPresent) {
      asn1Print_SpeedConfidence ("speedC", &pvalue->speedC);
   }

   if (pvalue->m.vehicleDataPresent) {
      asn1Print_VehicleStatus_vehicleData ("vehicleData", &pvalue->vehicleData
         );
   }

   if (pvalue->m.vehicleIdentPresent) {
      asn1Print_VehicleIdent ("vehicleIdent", &pvalue->vehicleIdent);
   }

   if (pvalue->m.j1939dataPresent) {
      asn1Print_J1939data ("j1939data", &pvalue->j1939data);
   }

   if (pvalue->m.weatherReportPresent) {
      asn1Print_VehicleStatus_weatherReport ("weatherReport", &pvalue->
         weatherReport);
   }

   if (pvalue->m.gnssStatusPresent) {
      asn1Print_GNSSstatus ("gnssStatus", &pvalue->gnssStatus);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_Snapshot (const char* name, const Snapshot* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_FullPositionVector ("thePosition", &pvalue->thePosition);

   if (pvalue->m.safetyExtPresent) {
      asn1Print_VehicleSafetyExtensions ("safetyExt", &pvalue->safetyExt);
   }

   if (pvalue->m.dataSetPresent) {
      asn1Print_VehicleStatus ("dataSet", &pvalue->dataSet);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_ProbeVehicleData_snapshots 
   (const char* name, const ProbeVehicleData_snapshots* pvalue)
{
   Snapshot* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((Snapshot*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_Snapshot (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_ProbeVehicleData_regional 
   (const char* name, const ProbeVehicleData_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_ProbeVehicleData 
   (const char* name, const ProbeVehicleData* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   if (pvalue->m.segNumPresent) {
      asn1Print_ProbeSegmentNumber ("segNum", &pvalue->segNum);
   }

   if (pvalue->m.probeIDPresent) {
      asn1Print_VehicleIdent ("probeID", &pvalue->probeID);
   }

   asn1Print_FullPositionVector ("startVector", &pvalue->startVector);

   asn1Print_VehicleClassification ("vehicleType", &pvalue->vehicleType);

   asn1Print_ProbeVehicleData_snapshots ("snapshots", &pvalue->snapshots);

   if (pvalue->m.regionalPresent) {
      asn1Print_ProbeVehicleData_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_AntennaOffsetSet 
   (const char* name, const AntennaOffsetSet* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Offset_B12 ("antOffsetX", &pvalue->antOffsetX);

   asn1Print_Offset_B09 ("antOffsetY", &pvalue->antOffsetY);

   asn1Print_Offset_B10 ("antOffsetZ", &pvalue->antOffsetZ);

   rtxPrintCloseBrace ();
}

void asn1Print_RTCMheader (const char* name, const RTCMheader* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_GNSSstatus ("status", &pvalue->status);

   asn1Print_AntennaOffsetSet ("offsetSet", &pvalue->offsetSet);

   rtxPrintCloseBrace ();
}

void asn1Print_RTCMmessageList 
   (const char* name, const RTCMmessageList* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RTCMmessage (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_RTCMcorrections_regional 
   (const char* name, const RTCMcorrections_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RTCMcorrections 
   (const char* name, const RTCMcorrections* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_MsgCount ("msgCnt", &pvalue->msgCnt);

   asn1Print_RTCM_Revision ("rev", &pvalue->rev);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   if (pvalue->m.anchorPointPresent) {
      asn1Print_FullPositionVector ("anchorPoint", &pvalue->anchorPoint);
   }

   if (pvalue->m.rtcmHeaderPresent) {
      asn1Print_RTCMheader ("rtcmHeader", &pvalue->rtcmHeader);
   }

   asn1Print_RTCMmessageList ("msgs", &pvalue->msgs);

   if (pvalue->m.regionalPresent) {
      asn1Print_RTCMcorrections_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_EnabledLaneList 
   (const char* name, const EnabledLaneList* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_LaneID (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_TimeChangeDetails 
   (const char* name, const TimeChangeDetails* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.startTimePresent) {
      asn1Print_TimeMark ("startTime", &pvalue->startTime);
   }

   asn1Print_TimeMark ("minEndTime", &pvalue->minEndTime);

   if (pvalue->m.maxEndTimePresent) {
      asn1Print_TimeMark ("maxEndTime", &pvalue->maxEndTime);
   }

   if (pvalue->m.likelyTimePresent) {
      asn1Print_TimeMark ("likelyTime", &pvalue->likelyTime);
   }

   if (pvalue->m.confidencePresent) {
      asn1Print_TimeIntervalConfidence ("confidence", &pvalue->confidence);
   }

   if (pvalue->m.nextTimePresent) {
      asn1Print_TimeMark ("nextTime", &pvalue->nextTime);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_AdvisorySpeed_regional 
   (const char* name, const AdvisorySpeed_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_AdvisorySpeed (const char* name, const AdvisorySpeed* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_AdvisorySpeedType ("type", &pvalue->type);

   if (pvalue->m.speedPresent) {
      asn1Print_SpeedAdvice ("speed", &pvalue->speed);
   }

   if (pvalue->m.confidencePresent) {
      asn1Print_SpeedConfidence ("confidence", &pvalue->confidence);
   }

   if (pvalue->m.distancePresent) {
      asn1Print_ZoneLength ("distance", &pvalue->distance);
   }

   if (pvalue->m.class_Present) {
      asn1Print_RestrictionClassID ("class_", &pvalue->class_);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_AdvisorySpeed_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_AdvisorySpeedList 
   (const char* name, const AdvisorySpeedList* pvalue)
{
   AdvisorySpeed* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((AdvisorySpeed*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_AdvisorySpeed (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_MovementEvent_regional 
   (const char* name, const MovementEvent_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_MovementEvent (const char* name, const MovementEvent* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_MovementPhaseState ("eventState", &pvalue->eventState);

   if (pvalue->m.timingPresent) {
      asn1Print_TimeChangeDetails ("timing", &pvalue->timing);
   }

   if (pvalue->m.speedsPresent) {
      asn1Print_AdvisorySpeedList ("speeds", &pvalue->speeds);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_MovementEvent_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_MovementEventList 
   (const char* name, const MovementEventList* pvalue)
{
   MovementEvent* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((MovementEvent*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_MovementEvent (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_ConnectionManeuverAssist_regional 
   (const char* name, const ConnectionManeuverAssist_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_ConnectionManeuverAssist 
   (const char* name, const ConnectionManeuverAssist* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_LaneConnectionID ("connectionID", &pvalue->connectionID);

   if (pvalue->m.queueLengthPresent) {
      asn1Print_ZoneLength ("queueLength", &pvalue->queueLength);
   }

   if (pvalue->m.availableStorageLengthPresent) {
      asn1Print_ZoneLength ("availableStorageLength", &pvalue->
         availableStorageLength);
   }

   if (pvalue->m.waitOnStopPresent) {
      asn1Print_WaitOnStopline ("waitOnStop", &pvalue->waitOnStop);
   }

   if (pvalue->m.pedBicycleDetectPresent) {
      asn1Print_PedestrianBicycleDetect ("pedBicycleDetect", &pvalue->
         pedBicycleDetect);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_ConnectionManeuverAssist_regional ("regional", &pvalue->
         regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_ManeuverAssistList 
   (const char* name, const ManeuverAssistList* pvalue)
{
   ConnectionManeuverAssist* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((ConnectionManeuverAssist*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_ConnectionManeuverAssist (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_MovementState_regional 
   (const char* name, const MovementState_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_MovementState (const char* name, const MovementState* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.movementNamePresent) {
      asn1Print_DescriptiveName ("movementName", pvalue->movementName);
   }

   asn1Print_SignalGroupID ("signalGroup", &pvalue->signalGroup);

   asn1Print_MovementEventList ("state_time_speed", &pvalue->state_time_speed
      );

   if (pvalue->m.maneuverAssistListPresent) {
      asn1Print_ManeuverAssistList ("maneuverAssistList", &pvalue->
         maneuverAssistList);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_MovementState_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_MovementList (const char* name, const MovementList* pvalue)
{
   MovementState* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((MovementState*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_MovementState (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_IntersectionState_regional 
   (const char* name, const IntersectionState_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_IntersectionState 
   (const char* name, const IntersectionState* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.namePresent) {
      asn1Print_DescriptiveName ("name", pvalue->name);
   }

   asn1Print_IntersectionReferenceID ("id", &pvalue->id);

   asn1Print_MsgCount ("revision", &pvalue->revision);

   asn1Print_IntersectionStatusObject ("status", &pvalue->status);

   if (pvalue->m.moyPresent) {
      asn1Print_MinuteOfTheYear ("moy", &pvalue->moy);
   }

   if (pvalue->m.timeStampPresent) {
      asn1Print_DSecond ("timeStamp", &pvalue->timeStamp);
   }

   if (pvalue->m.enabledLanesPresent) {
      asn1Print_EnabledLaneList ("enabledLanes", &pvalue->enabledLanes);
   }

   asn1Print_MovementList ("states", &pvalue->states);

   if (pvalue->m.maneuverAssistListPresent) {
      asn1Print_ManeuverAssistList ("maneuverAssistList", &pvalue->
         maneuverAssistList);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_IntersectionState_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_IntersectionStateList 
   (const char* name, const IntersectionStateList* pvalue)
{
   IntersectionState* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((IntersectionState*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_IntersectionState (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SPAT_regional (const char* name, const SPAT_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SPAT (const char* name, const SPAT* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   if (pvalue->m.namePresent) {
      asn1Print_DescriptiveName ("name", pvalue->name);
   }

   asn1Print_IntersectionStateList ("intersections", &pvalue->intersections);

   if (pvalue->m.regionalPresent) {
      asn1Print_SPAT_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_IntersectionAccessPoint 
   (const char* name, const IntersectionAccessPoint* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* lane */
      case 1:
      {
         asn1Print_LaneID ("lane", &pvalue->u.lane);
         break;
      }
      /* approach */
      case 2:
      {
         asn1Print_ApproachID ("approach", &pvalue->u.approach);
         break;
      }
      /* connection */
      case 3:
      {
         asn1Print_LaneConnectionID ("connection", &pvalue->u.connection);
         break;
      }
      /* extElem1 */
      case 4:
      {
         rtxPrintIndent ();
         rtxPrintHexStr ("extElem1", pvalue->u.extElem1->numocts,
         pvalue->u.extElem1->data);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_SignalRequest_regional 
   (const char* name, const SignalRequest_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SignalRequest (const char* name, const SignalRequest* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_IntersectionReferenceID ("id", &pvalue->id);

   asn1Print_RequestID ("requestID", &pvalue->requestID);

   asn1Print_PriorityRequestType ("requestType", &pvalue->requestType);

   asn1Print_IntersectionAccessPoint ("inBoundLane", &pvalue->inBoundLane);

   if (pvalue->m.outBoundLanePresent) {
      asn1Print_IntersectionAccessPoint ("outBoundLane", &pvalue->outBoundLane
         );
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_SignalRequest_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_SignalRequestPackage_regional 
   (const char* name, const SignalRequestPackage_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SignalRequestPackage 
   (const char* name, const SignalRequestPackage* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_SignalRequest ("request", &pvalue->request);

   if (pvalue->m.minutePresent) {
      asn1Print_MinuteOfTheYear ("minute", &pvalue->minute);
   }

   if (pvalue->m.secondPresent) {
      asn1Print_DSecond ("second", &pvalue->second);
   }

   if (pvalue->m.durationPresent) {
      asn1Print_DSecond ("duration", &pvalue->duration);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_SignalRequestPackage_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_SignalRequestList 
   (const char* name, const SignalRequestList* pvalue)
{
   SignalRequestPackage* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((SignalRequestPackage*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_SignalRequestPackage (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RequestorType (const char* name, const RequestorType* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_BasicVehicleRole ("role", &pvalue->role);

   if (pvalue->m.subrolePresent) {
      asn1Print_RequestSubRole ("subrole", &pvalue->subrole);
   }

   if (pvalue->m.requestPresent) {
      asn1Print_RequestImportanceLevel ("request", &pvalue->request);
   }

   if (pvalue->m.iso3883Present) {
      asn1Print_Iso3833VehicleType ("iso3883", &pvalue->iso3883);
   }

   if (pvalue->m.hpmsTypePresent) {
      asn1Print_VehicleType ("hpmsType", &pvalue->hpmsType);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RequestorPositionVector 
   (const char* name, const RequestorPositionVector* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Position3D ("position", &pvalue->position);

   if (pvalue->m.headingPresent) {
      asn1Print_Angle ("heading", &pvalue->heading);
   }

   if (pvalue->m.speedPresent) {
      asn1Print_TransmissionAndSpeed ("speed", &pvalue->speed);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RequestorDescription_regional 
   (const char* name, const RequestorDescription_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RequestorDescription 
   (const char* name, const RequestorDescription* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_VehicleID ("id", &pvalue->id);

   if (pvalue->m.typePresent) {
      asn1Print_RequestorType ("type", &pvalue->type);
   }

   if (pvalue->m.positionPresent) {
      asn1Print_RequestorPositionVector ("position", &pvalue->position);
   }

   if (pvalue->m.namePresent) {
      asn1Print_DescriptiveName ("name", pvalue->name);
   }

   if (pvalue->m.routeNamePresent) {
      asn1Print_DescriptiveName ("routeName", pvalue->routeName);
   }

   if (pvalue->m.transitStatusPresent) {
      asn1Print_TransitVehicleStatus ("transitStatus", &pvalue->transitStatus
         );
   }

   if (pvalue->m.transitOccupancyPresent) {
      asn1Print_TransitVehicleOccupancy ("transitOccupancy", &pvalue->
         transitOccupancy);
   }

   if (pvalue->m.transitSchedulePresent) {
      asn1Print_DeltaTime ("transitSchedule", &pvalue->transitSchedule);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RequestorDescription_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_SignalRequestMessage_regional 
   (const char* name, const SignalRequestMessage_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SignalRequestMessage 
   (const char* name, const SignalRequestMessage* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   asn1Print_DSecond ("second", &pvalue->second);

   if (pvalue->m.sequenceNumberPresent) {
      asn1Print_MsgCount ("sequenceNumber", &pvalue->sequenceNumber);
   }

   if (pvalue->m.requestsPresent) {
      asn1Print_SignalRequestList ("requests", &pvalue->requests);
   }

   asn1Print_RequestorDescription ("requestor", &pvalue->requestor);

   if (pvalue->m.regionalPresent) {
      asn1Print_SignalRequestMessage_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_SignalRequesterInfo 
   (const char* name, const SignalRequesterInfo* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_VehicleID ("id", &pvalue->id);

   asn1Print_RequestID ("request", &pvalue->request);

   asn1Print_MsgCount ("sequenceNumber", &pvalue->sequenceNumber);

   if (pvalue->m.rolePresent) {
      asn1Print_BasicVehicleRole ("role", &pvalue->role);
   }

   if (pvalue->m.typeDataPresent) {
      asn1Print_RequestorType ("typeData", &pvalue->typeData);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_SignalStatusPackage_regional 
   (const char* name, const SignalStatusPackage_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SignalStatusPackage 
   (const char* name, const SignalStatusPackage* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.requesterPresent) {
      asn1Print_SignalRequesterInfo ("requester", &pvalue->requester);
   }

   asn1Print_IntersectionAccessPoint ("inboundOn", &pvalue->inboundOn);

   if (pvalue->m.outboundOnPresent) {
      asn1Print_IntersectionAccessPoint ("outboundOn", &pvalue->outboundOn);
   }

   if (pvalue->m.minutePresent) {
      asn1Print_MinuteOfTheYear ("minute", &pvalue->minute);
   }

   if (pvalue->m.secondPresent) {
      asn1Print_DSecond ("second", &pvalue->second);
   }

   if (pvalue->m.durationPresent) {
      asn1Print_DSecond ("duration", &pvalue->duration);
   }

   asn1Print_PrioritizationResponseStatus ("status", &pvalue->status);

   if (pvalue->m.regionalPresent) {
      asn1Print_SignalStatusPackage_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_SignalStatusPackageList 
   (const char* name, const SignalStatusPackageList* pvalue)
{
   SignalStatusPackage* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((SignalStatusPackage*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_SignalStatusPackage (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SignalStatus_regional 
   (const char* name, const SignalStatus_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SignalStatus (const char* name, const SignalStatus* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_MsgCount ("sequenceNumber", &pvalue->sequenceNumber);

   asn1Print_IntersectionReferenceID ("id", &pvalue->id);

   asn1Print_SignalStatusPackageList ("sigStatus", &pvalue->sigStatus);

   if (pvalue->m.regionalPresent) {
      asn1Print_SignalStatus_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_SignalStatusList 
   (const char* name, const SignalStatusList* pvalue)
{
   SignalStatus* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((SignalStatus*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_SignalStatus (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SignalStatusMessage_regional 
   (const char* name, const SignalStatusMessage_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SignalStatusMessage 
   (const char* name, const SignalStatusMessage* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   asn1Print_DSecond ("second", &pvalue->second);

   if (pvalue->m.sequenceNumberPresent) {
      asn1Print_MsgCount ("sequenceNumber", &pvalue->sequenceNumber);
   }

   asn1Print_SignalStatusList ("status", &pvalue->status);

   if (pvalue->m.regionalPresent) {
      asn1Print_SignalStatusMessage_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RoadSignID (const char* name, const RoadSignID* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Position3D ("position", &pvalue->position);

   asn1Print_HeadingSlice ("viewAngle", &pvalue->viewAngle);

   if (pvalue->m.mutcdCodePresent) {
      asn1Print_MUTCDCode ("mutcdCode", &pvalue->mutcdCode);
   }

   if (pvalue->m.crcPresent) {
      asn1Print_MsgCRC ("crc", &pvalue->crc);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_TravelerDataFrame_msgId 
   (const char* name, const TravelerDataFrame_msgId* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* furtherInfoID */
      case 1:
      {
         asn1Print_FurtherInfoID ("furtherInfoID", pvalue->u.furtherInfoID);
         break;
      }
      /* roadSignID */
      case 2:
      {
         asn1Print_RoadSignID ("roadSignID", pvalue->u.roadSignID);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_Node_LL_24B (const char* name, const Node_LL_24B* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_OffsetLL_B12 ("lon", &pvalue->lon);

   asn1Print_OffsetLL_B12 ("lat", &pvalue->lat);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_LL_28B (const char* name, const Node_LL_28B* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_OffsetLL_B14 ("lon", &pvalue->lon);

   asn1Print_OffsetLL_B14 ("lat", &pvalue->lat);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_LL_32B (const char* name, const Node_LL_32B* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_OffsetLL_B16 ("lon", &pvalue->lon);

   asn1Print_OffsetLL_B16 ("lat", &pvalue->lat);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_LL_36B (const char* name, const Node_LL_36B* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_OffsetLL_B18 ("lon", &pvalue->lon);

   asn1Print_OffsetLL_B18 ("lat", &pvalue->lat);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_LL_44B (const char* name, const Node_LL_44B* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_OffsetLL_B22 ("lon", &pvalue->lon);

   asn1Print_OffsetLL_B22 ("lat", &pvalue->lat);

   rtxPrintCloseBrace ();
}

void asn1Print_Node_LL_48B (const char* name, const Node_LL_48B* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_OffsetLL_B24 ("lon", &pvalue->lon);

   asn1Print_OffsetLL_B24 ("lat", &pvalue->lat);

   rtxPrintCloseBrace ();
}

void asn1Print_NodeOffsetPointLL 
   (const char* name, const NodeOffsetPointLL* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* node-LL1 */
      case 1:
      {
         asn1Print_Node_LL_24B ("node-LL1", pvalue->u.node_LL1);
         break;
      }
      /* node-LL2 */
      case 2:
      {
         asn1Print_Node_LL_28B ("node-LL2", pvalue->u.node_LL2);
         break;
      }
      /* node-LL3 */
      case 3:
      {
         asn1Print_Node_LL_32B ("node-LL3", pvalue->u.node_LL3);
         break;
      }
      /* node-LL4 */
      case 4:
      {
         asn1Print_Node_LL_36B ("node-LL4", pvalue->u.node_LL4);
         break;
      }
      /* node-LL5 */
      case 5:
      {
         asn1Print_Node_LL_44B ("node-LL5", pvalue->u.node_LL5);
         break;
      }
      /* node-LL6 */
      case 6:
      {
         asn1Print_Node_LL_48B ("node-LL6", pvalue->u.node_LL6);
         break;
      }
      /* node-LatLon */
      case 7:
      {
         asn1Print_Node_LLmD_64b ("node-LatLon", pvalue->u.node_LatLon);
         break;
      }
      /* regional */
      case 8:
      {
         asn1Print_RegionalExtension ("regional", pvalue->u.regional);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_NodeAttributeLLList 
   (const char* name, const NodeAttributeLLList* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_NodeAttributeLL (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_SegmentAttributeLLList 
   (const char* name, const SegmentAttributeLLList* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_SegmentAttributeLL (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_NodeAttributeSetLL_regional 
   (const char* name, const NodeAttributeSetLL_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_NodeAttributeSetLL 
   (const char* name, const NodeAttributeSetLL* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.localNodePresent) {
      asn1Print_NodeAttributeLLList ("localNode", &pvalue->localNode);
   }

   if (pvalue->m.disabledPresent) {
      asn1Print_SegmentAttributeLLList ("disabled", &pvalue->disabled);
   }

   if (pvalue->m.enabledPresent) {
      asn1Print_SegmentAttributeLLList ("enabled", &pvalue->enabled);
   }

   if (pvalue->m.dataPresent) {
      asn1Print_LaneDataAttributeList ("data", &pvalue->data);
   }

   if (pvalue->m.dWidthPresent) {
      asn1Print_Offset_B10 ("dWidth", &pvalue->dWidth);
   }

   if (pvalue->m.dElevationPresent) {
      asn1Print_Offset_B10 ("dElevation", &pvalue->dElevation);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_NodeAttributeSetLL_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_NodeLL (const char* name, const NodeLL* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_NodeOffsetPointLL ("delta", &pvalue->delta);

   if (pvalue->m.attributesPresent) {
      asn1Print_NodeAttributeSetLL ("attributes", &pvalue->attributes);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_NodeSetLL (const char* name, const NodeSetLL* pvalue)
{
   NodeLL* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((NodeLL*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_NodeLL (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_NodeListLL (const char* name, const NodeListLL* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* nodes */
      case 1:
      {
         asn1Print_NodeSetLL ("nodes", pvalue->u.nodes);
         break;
      }
      /* extElem1 */
      case 2:
      {
         rtxPrintIndent ();
         rtxPrintHexStr ("extElem1", pvalue->u.extElem1->numocts,
         pvalue->u.extElem1->data);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_OffsetSystem_offset 
   (const char* name, const OffsetSystem_offset* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* xy */
      case 1:
      {
         asn1Print_NodeListXY ("xy", pvalue->u.xy);
         break;
      }
      /* ll */
      case 2:
      {
         asn1Print_NodeListLL ("ll", pvalue->u.ll);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_OffsetSystem (const char* name, const OffsetSystem* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.scalePresent) {
      asn1Print_Zoom ("scale", &pvalue->scale);
   }

   asn1Print_OffsetSystem_offset ("offset", &pvalue->offset);

   rtxPrintCloseBrace ();
}

void asn1Print_Circle (const char* name, const Circle* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Position3D ("center", &pvalue->center);

   asn1Print_Radius_B12 ("radius", &pvalue->radius);

   asn1Print_DistanceUnits ("units", &pvalue->units);

   rtxPrintCloseBrace ();
}

void asn1Print_GeometricProjection_regional 
   (const char* name, const GeometricProjection_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_GeometricProjection 
   (const char* name, const GeometricProjection* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_HeadingSlice ("direction", &pvalue->direction);

   if (pvalue->m.extentPresent) {
      asn1Print_Extent ("extent", &pvalue->extent);
   }

   if (pvalue->m.laneWidthPresent) {
      asn1Print_LaneWidth ("laneWidth", &pvalue->laneWidth);
   }

   asn1Print_Circle ("circle", &pvalue->circle);

   if (pvalue->m.regionalPresent) {
      asn1Print_GeometricProjection_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_ShapePointSet (const char* name, const ShapePointSet* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.anchorPresent) {
      asn1Print_Position3D ("anchor", &pvalue->anchor);
   }

   if (pvalue->m.laneWidthPresent) {
      asn1Print_LaneWidth ("laneWidth", &pvalue->laneWidth);
   }

   if (pvalue->m.directionalityPresent) {
      asn1Print_DirectionOfUse ("directionality", &pvalue->directionality);
   }

   asn1Print_NodeListXY ("nodeList", &pvalue->nodeList);

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RegionOffsets (const char* name, const RegionOffsets* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_OffsetLL_B16 ("xOffset", &pvalue->xOffset);

   asn1Print_OffsetLL_B16 ("yOffset", &pvalue->yOffset);

   if (pvalue->m.zOffsetPresent) {
      asn1Print_OffsetLL_B16 ("zOffset", &pvalue->zOffset);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_RegionList (const char* name, const RegionList* pvalue)
{
   RegionOffsets* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionOffsets*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionOffsets (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_RegionPointSet (const char* name, const RegionPointSet* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.anchorPresent) {
      asn1Print_Position3D ("anchor", &pvalue->anchor);
   }

   if (pvalue->m.scalePresent) {
      asn1Print_Zoom ("scale", &pvalue->scale);
   }

   asn1Print_RegionList ("nodeList", &pvalue->nodeList);

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_ValidRegion_area 
   (const char* name, const ValidRegion_area* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* shapePointSet */
      case 1:
      {
         asn1Print_ShapePointSet ("shapePointSet", pvalue->u.shapePointSet);
         break;
      }
      /* circle */
      case 2:
      {
         asn1Print_Circle ("circle", pvalue->u.circle);
         break;
      }
      /* regionPointSet */
      case 3:
      {
         asn1Print_RegionPointSet ("regionPointSet", pvalue->u.regionPointSet
            );
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_ValidRegion (const char* name, const ValidRegion* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_HeadingSlice ("direction", &pvalue->direction);

   if (pvalue->m.extentPresent) {
      asn1Print_Extent ("extent", &pvalue->extent);
   }

   asn1Print_ValidRegion_area ("area", &pvalue->area);

   rtxPrintCloseBrace ();
}

void asn1Print_GeographicalPath_description 
   (const char* name, const GeographicalPath_description* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* path */
      case 1:
      {
         asn1Print_OffsetSystem ("path", pvalue->u.path);
         break;
      }
      /* geometry */
      case 2:
      {
         asn1Print_GeometricProjection ("geometry", pvalue->u.geometry);
         break;
      }
      /* oldRegion */
      case 3:
      {
         asn1Print_ValidRegion ("oldRegion", pvalue->u.oldRegion);
         break;
      }
      /* extElem1 */
      case 4:
      {
         rtxPrintIndent ();
         rtxPrintHexStr ("extElem1", pvalue->u.extElem1->numocts,
         pvalue->u.extElem1->data);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_GeographicalPath_regional 
   (const char* name, const GeographicalPath_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_GeographicalPath 
   (const char* name, const GeographicalPath* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.namePresent) {
      asn1Print_DescriptiveName ("name", pvalue->name);
   }

   if (pvalue->m.idPresent) {
      asn1Print_RoadSegmentReferenceID ("id", &pvalue->id);
   }

   if (pvalue->m.anchorPresent) {
      asn1Print_Position3D ("anchor", &pvalue->anchor);
   }

   if (pvalue->m.laneWidthPresent) {
      asn1Print_LaneWidth ("laneWidth", &pvalue->laneWidth);
   }

   if (pvalue->m.directionalityPresent) {
      asn1Print_DirectionOfUse ("directionality", &pvalue->directionality);
   }

   if (pvalue->m.closedPathPresent) {
      rtxPrintIndent ();
      rtxPrintBoolean ("closedPath", pvalue->closedPath);
   }

   if (pvalue->m.directionPresent) {
      asn1Print_HeadingSlice ("direction", &pvalue->direction);
   }

   if (pvalue->m.descriptionPresent) {
      asn1Print_GeographicalPath_description ("description", &pvalue->
         description);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_GeographicalPath_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TravelerDataFrame_regions 
   (const char* name, const TravelerDataFrame_regions* pvalue)
{
   GeographicalPath* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((GeographicalPath*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_GeographicalPath (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_WorkZone_element_item 
   (const char* name, const WorkZone_element_item* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* itis */
      case 1:
      {
         asn1Print_ITIScodes ("itis", &pvalue->u.itis);
         break;
      }
      /* text */
      case 2:
      {
         asn1Print_ITIStextPhrase ("text", pvalue->u.text);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_WorkZone_element 
   (const char* name, const WorkZone_element* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_WorkZone_element_item ("item", &pvalue->item);

   rtxPrintCloseBrace ();
}

void asn1Print_WorkZone (const char* name, const WorkZone* pvalue)
{
   WorkZone_element* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((WorkZone_element*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_WorkZone_element (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_GenericSignage_element_item 
   (const char* name, const GenericSignage_element_item* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* itis */
      case 1:
      {
         asn1Print_ITIScodes ("itis", &pvalue->u.itis);
         break;
      }
      /* text */
      case 2:
      {
         asn1Print_ITIStextPhrase ("text", pvalue->u.text);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_GenericSignage_element 
   (const char* name, const GenericSignage_element* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_GenericSignage_element_item ("item", &pvalue->item);

   rtxPrintCloseBrace ();
}

void asn1Print_GenericSignage (const char* name, const GenericSignage* pvalue)
{
   GenericSignage_element* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((GenericSignage_element*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_GenericSignage_element (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SpeedLimit_element_item 
   (const char* name, const SpeedLimit_element_item* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* itis */
      case 1:
      {
         asn1Print_ITIScodes ("itis", &pvalue->u.itis);
         break;
      }
      /* text */
      case 2:
      {
         asn1Print_ITIStextPhrase ("text", pvalue->u.text);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_SpeedLimit_element 
   (const char* name, const SpeedLimit_element* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_SpeedLimit_element_item ("item", &pvalue->item);

   rtxPrintCloseBrace ();
}

void asn1Print_SpeedLimit (const char* name, const SpeedLimit* pvalue)
{
   SpeedLimit_element* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((SpeedLimit_element*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_SpeedLimit_element (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_ExitService_element_item 
   (const char* name, const ExitService_element_item* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* itis */
      case 1:
      {
         asn1Print_ITIScodes ("itis", &pvalue->u.itis);
         break;
      }
      /* text */
      case 2:
      {
         asn1Print_ITIStextPhrase ("text", pvalue->u.text);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_ExitService_element 
   (const char* name, const ExitService_element* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_ExitService_element_item ("item", &pvalue->item);

   rtxPrintCloseBrace ();
}

void asn1Print_ExitService (const char* name, const ExitService* pvalue)
{
   ExitService_element* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((ExitService_element*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_ExitService_element (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_TravelerDataFrame_content 
   (const char* name, const TravelerDataFrame_content* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* advisory */
      case 1:
      {
         asn1Print_ITIScodesAndText ("advisory", pvalue->u.advisory);
         break;
      }
      /* workZone */
      case 2:
      {
         asn1Print_WorkZone ("workZone", pvalue->u.workZone);
         break;
      }
      /* genericSign */
      case 3:
      {
         asn1Print_GenericSignage ("genericSign", pvalue->u.genericSign);
         break;
      }
      /* speedLimit */
      case 4:
      {
         asn1Print_SpeedLimit ("speedLimit", pvalue->u.speedLimit);
         break;
      }
      /* exitService */
      case 5:
      {
         asn1Print_ExitService ("exitService", pvalue->u.exitService);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

void asn1Print_TravelerDataFrame 
   (const char* name, const TravelerDataFrame* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_SSPindex ("sspTimRights", &pvalue->sspTimRights);

   asn1Print_TravelerInfoType ("frameType", &pvalue->frameType);

   asn1Print_TravelerDataFrame_msgId ("msgId", &pvalue->msgId);

   if (pvalue->m.startYearPresent) {
      asn1Print_DYear ("startYear", &pvalue->startYear);
   }

   asn1Print_MinuteOfTheYear ("startTime", &pvalue->startTime);

   asn1Print_MinutesDuration ("duratonTime", &pvalue->duratonTime);

   asn1Print_SignPrority ("priority", &pvalue->priority);

   asn1Print_SSPindex ("sspLocationRights", &pvalue->sspLocationRights);

   asn1Print_TravelerDataFrame_regions ("regions", &pvalue->regions);

   asn1Print_SSPindex ("sspMsgRights1", &pvalue->sspMsgRights1);

   asn1Print_SSPindex ("sspMsgRights2", &pvalue->sspMsgRights2);

   asn1Print_TravelerDataFrame_content ("content", &pvalue->content);

   if (pvalue->m.urlPresent) {
      asn1Print_URL_Short ("url", pvalue->url);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TravelerDataFrameList 
   (const char* name, const TravelerDataFrameList* pvalue)
{
   TravelerDataFrame* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((TravelerDataFrame*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_TravelerDataFrame (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_TravelerInformation_regional 
   (const char* name, const TravelerInformation_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_TravelerInformation 
   (const char* name, const TravelerInformation* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_MsgCount ("msgCnt", &pvalue->msgCnt);

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   if (pvalue->m.packetIDPresent) {
      asn1Print_UniqueMSGID ("packetID", &pvalue->packetID);
   }

   if (pvalue->m.urlBPresent) {
      asn1Print_URL_Base ("urlB", pvalue->urlB);
   }

   asn1Print_TravelerDataFrameList ("dataFrames", &pvalue->dataFrames);

   if (pvalue->m.regionalPresent) {
      asn1Print_TravelerInformation_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_Header (const char* name, const Header* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.yearPresent) {
      asn1Print_DYear ("year", &pvalue->year);
   }

   if (pvalue->m.timeStampPresent) {
      asn1Print_MinuteOfTheYear ("timeStamp", &pvalue->timeStamp);
   }

   if (pvalue->m.secMarkPresent) {
      asn1Print_DSecond ("secMark", &pvalue->secMark);
   }

   if (pvalue->m.msgIssueRevisionPresent) {
      asn1Print_MsgCount ("msgIssueRevision", &pvalue->msgIssueRevision);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage00 (const char* name, const TestMessage00* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage01 (const char* name, const TestMessage01* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage02 (const char* name, const TestMessage02* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage03 (const char* name, const TestMessage03* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage04 (const char* name, const TestMessage04* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage05 (const char* name, const TestMessage05* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage06 (const char* name, const TestMessage06* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage07 (const char* name, const TestMessage07* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage08 (const char* name, const TestMessage08* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage09 (const char* name, const TestMessage09* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage10 (const char* name, const TestMessage10* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage11 (const char* name, const TestMessage11* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage12 (const char* name, const TestMessage12* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage13 (const char* name, const TestMessage13* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage14 (const char* name, const TestMessage14* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TestMessage15 (const char* name, const TestMessage15* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.headerPresent) {
      asn1Print_Header ("header", &pvalue->header);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_RegionalExtension ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_DDate (const char* name, const DDate* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_DYear ("year", &pvalue->year);

   asn1Print_DMonth ("month", &pvalue->month);

   asn1Print_DDay ("day", &pvalue->day);

   rtxPrintCloseBrace ();
}

void asn1Print_DFullTime (const char* name, const DFullTime* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_DYear ("year", &pvalue->year);

   asn1Print_DMonth ("month", &pvalue->month);

   asn1Print_DDay ("day", &pvalue->day);

   asn1Print_DHour ("hour", &pvalue->hour);

   asn1Print_DMinute ("minute", &pvalue->minute);

   rtxPrintCloseBrace ();
}

void asn1Print_DMonthDay (const char* name, const DMonthDay* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_DMonth ("month", &pvalue->month);

   asn1Print_DDay ("day", &pvalue->day);

   rtxPrintCloseBrace ();
}

void asn1Print_DTime (const char* name, const DTime* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_DHour ("hour", &pvalue->hour);

   asn1Print_DMinute ("minute", &pvalue->minute);

   asn1Print_DSecond ("second", &pvalue->second);

   if (pvalue->m.offsetPresent) {
      asn1Print_DOffset ("offset", &pvalue->offset);
   }

   rtxPrintCloseBrace ();
}

void asn1Print_DYearMonth (const char* name, const DYearMonth* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_DYear ("year", &pvalue->year);

   asn1Print_DMonth ("month", &pvalue->month);

   rtxPrintCloseBrace ();
}

void asn1Print_DisabledVehicle 
   (const char* name, const DisabledVehicle* pvalue)
{
   rtxPrintOpenBrace (name);

   rtxPrintIndent ();
   rtxPrintUnsigned ("statusDetails", pvalue->statusDetails);

   if (pvalue->m.locationDetailsPresent) {
      asn1Print_GenericLocations ("locationDetails", &pvalue->locationDetails
         );
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_EventDescription_description 
   (const char* name, const EventDescription_description* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_ITIScodes (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_EventDescription_regional 
   (const char* name, const EventDescription_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_EventDescription 
   (const char* name, const EventDescription* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_ITIScodes ("typeEvent", &pvalue->typeEvent);

   if (pvalue->m.descriptionPresent) {
      asn1Print_EventDescription_description ("description", &pvalue->
         description);
   }

   if (pvalue->m.priorityPresent) {
      asn1Print_Priority ("priority", &pvalue->priority);
   }

   if (pvalue->m.headingPresent) {
      asn1Print_HeadingSlice ("heading", &pvalue->heading);
   }

   if (pvalue->m.extentPresent) {
      asn1Print_Extent ("extent", &pvalue->extent);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_EventDescription_regional ("regional", &pvalue->regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_ObstacleDetection 
   (const char* name, const ObstacleDetection* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_ObstacleDistance ("obDist", &pvalue->obDist);

   asn1Print_ObstacleDirection ("obDirect", &pvalue->obDirect);

   if (pvalue->m.descriptionPresent) {
      rtxPrintIndent ();
      rtxPrintUnsigned ("description", pvalue->description);
   }

   if (pvalue->m.locationDetailsPresent) {
      asn1Print_GenericLocations ("locationDetails", &pvalue->locationDetails
         );
   }

   asn1Print_DDateTime ("dateTime", &pvalue->dateTime);

   if (pvalue->m.vertEventPresent) {
      asn1Print_VerticalAccelerationThreshold ("vertEvent", &pvalue->vertEvent
         );
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_PivotPointDescription 
   (const char* name, const PivotPointDescription* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Offset_B11 ("pivotOffset", &pvalue->pivotOffset);

   asn1Print_Angle ("pivotAngle", &pvalue->pivotAngle);

   asn1Print_PivotingAllowed ("pivots", &pvalue->pivots);

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_RTCMPackage (const char* name, const RTCMPackage* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.rtcmHeaderPresent) {
      asn1Print_RTCMheader ("rtcmHeader", &pvalue->rtcmHeader);
   }

   asn1Print_RTCMmessageList ("msgs", &pvalue->msgs);

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TrailerHistoryPoint 
   (const char* name, const TrailerHistoryPoint* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_Angle ("pivotAngle", &pvalue->pivotAngle);

   asn1Print_TimeOffset ("timeOffset", &pvalue->timeOffset);

   asn1Print_Node_XY_24b ("positionOffset", &pvalue->positionOffset);

   if (pvalue->m.elevationOffsetPresent) {
      asn1Print_VertOffset_B07 ("elevationOffset", &pvalue->elevationOffset);
   }

   if (pvalue->m.headingPresent) {
      asn1Print_CoarseHeading ("heading", &pvalue->heading);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TrailerHistoryPointList 
   (const char* name, const TrailerHistoryPointList* pvalue)
{
   TrailerHistoryPoint* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((TrailerHistoryPoint*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_TrailerHistoryPoint (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_TrailerUnitDescription 
   (const char* name, const TrailerUnitDescription* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_IsDolly ("isDolly", &pvalue->isDolly);

   asn1Print_VehicleWidth ("width", &pvalue->width);

   asn1Print_VehicleLength ("length", &pvalue->length);

   if (pvalue->m.heightPresent) {
      asn1Print_VehicleHeight ("height", &pvalue->height);
   }

   if (pvalue->m.massPresent) {
      asn1Print_TrailerMass ("mass", &pvalue->mass);
   }

   if (pvalue->m.bumperHeightsPresent) {
      asn1Print_BumperHeights ("bumperHeights", &pvalue->bumperHeights);
   }

   if (pvalue->m.centerOfGravityPresent) {
      asn1Print_VehicleHeight ("centerOfGravity", &pvalue->centerOfGravity);
   }

   asn1Print_PivotPointDescription ("frontPivot", &pvalue->frontPivot);

   if (pvalue->m.rearPivotPresent) {
      asn1Print_PivotPointDescription ("rearPivot", &pvalue->rearPivot);
   }

   if (pvalue->m.rearWheelOffsetPresent) {
      asn1Print_Offset_B12 ("rearWheelOffset", &pvalue->rearWheelOffset);
   }

   asn1Print_Node_XY_24b ("positionOffset", &pvalue->positionOffset);

   if (pvalue->m.elevationOffsetPresent) {
      asn1Print_VertOffset_B07 ("elevationOffset", &pvalue->elevationOffset);
   }

   if (pvalue->m.crumbDataPresent) {
      asn1Print_TrailerHistoryPointList ("crumbData", &pvalue->crumbData);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_TrailerUnitDescriptionList 
   (const char* name, const TrailerUnitDescriptionList* pvalue)
{
   TrailerUnitDescription* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((TrailerUnitDescription*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_TrailerUnitDescription (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_TrailerData (const char* name, const TrailerData* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_SSPindex ("sspRights", &pvalue->sspRights);

   asn1Print_PivotPointDescription ("connection", &pvalue->connection);

   asn1Print_TrailerUnitDescriptionList ("units", &pvalue->units);

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_SpecialVehicleExtensions 
   (const char* name, const SpecialVehicleExtensions* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.vehicleAlertsPresent) {
      asn1Print_EmergencyDetails ("vehicleAlerts", &pvalue->vehicleAlerts);
   }

   if (pvalue->m.descriptionPresent) {
      asn1Print_EventDescription ("description", &pvalue->description);
   }

   if (pvalue->m.trailersPresent) {
      asn1Print_TrailerData ("trailers", &pvalue->trailers);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_SpeedProfileMeasurementList 
   (const char* name, const SpeedProfileMeasurementList* pvalue)
{
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (; xx1 < pvalue->n; xx1++) {
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_SpeedProfileMeasurement (namebuf, &pvalue->elem[xx1]);
   }
}

void asn1Print_SpeedProfile (const char* name, const SpeedProfile* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_SpeedProfileMeasurementList ("speedReports", &pvalue->speedReports
      );

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_VehicleData (const char* name, const VehicleData* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.heightPresent) {
      asn1Print_VehicleHeight ("height", &pvalue->height);
   }

   if (pvalue->m.bumpersPresent) {
      asn1Print_BumperHeights ("bumpers", &pvalue->bumpers);
   }

   if (pvalue->m.massPresent) {
      asn1Print_VehicleMass ("mass", &pvalue->mass);
   }

   if (pvalue->m.trailerWeightPresent) {
      asn1Print_TrailerWeight ("trailerWeight", &pvalue->trailerWeight);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_WeatherReport (const char* name, const WeatherReport* pvalue)
{
   rtxPrintOpenBrace (name);

   asn1Print_EssPrecipYesNo ("isRaining", &pvalue->isRaining);

   if (pvalue->m.rainRatePresent) {
      asn1Print_EssPrecipRate ("rainRate", &pvalue->rainRate);
   }

   if (pvalue->m.precipSituationPresent) {
      asn1Print_EssPrecipSituation ("precipSituation", &pvalue->precipSituation
         );
   }

   if (pvalue->m.solarRadiationPresent) {
      asn1Print_EssSolarRadiation ("solarRadiation", &pvalue->solarRadiation);
   }

   if (pvalue->m.frictionPresent) {
      asn1Print_EssMobileFriction ("friction", &pvalue->friction);
   }

   if (pvalue->m.roadFrictionPresent) {
      asn1Print_CoefficientOfFriction ("roadFriction", &pvalue->roadFriction);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_WeatherProbe (const char* name, const WeatherProbe* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.airTempPresent) {
      asn1Print_AmbientAirTemperature ("airTemp", &pvalue->airTemp);
   }

   if (pvalue->m.airPressurePresent) {
      asn1Print_AmbientAirPressure ("airPressure", &pvalue->airPressure);
   }

   if (pvalue->m.rainRatesPresent) {
      asn1Print_WiperSet ("rainRates", &pvalue->rainRates);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_SupplementalVehicleExtensions_regional 
   (const char* name, const SupplementalVehicleExtensions_regional* pvalue)
{
   RegionalExtension* pdata0;
   OSRTDListNode* pnode;
   char namebuf[512];
   char numbuf[32];
   OSUINT32 xx1 = 0;

   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      pdata0 = ((RegionalExtension*)pnode->data);
      rtxUIntToCharStr (xx1, numbuf, sizeof(numbuf), 0);
      rtxStrJoin (namebuf, sizeof(namebuf), name, "[", numbuf, "]", 0);
      asn1Print_RegionalExtension (namebuf, pdata0);
      xx1++;
   }
}

void asn1Print_SupplementalVehicleExtensions 
   (const char* name, const SupplementalVehicleExtensions* pvalue)
{
   rtxPrintOpenBrace (name);

   if (pvalue->m.classificationPresent) {
      asn1Print_BasicVehicleClass ("classification", &pvalue->classification);
   }

   if (pvalue->m.classDetailsPresent) {
      asn1Print_VehicleClassification ("classDetails", &pvalue->classDetails);
   }

   if (pvalue->m.vehicleDataPresent) {
      asn1Print_VehicleData ("vehicleData", &pvalue->vehicleData);
   }

   if (pvalue->m.weatherReportPresent) {
      asn1Print_WeatherReport ("weatherReport", &pvalue->weatherReport);
   }

   if (pvalue->m.weatherProbePresent) {
      asn1Print_WeatherProbe ("weatherProbe", &pvalue->weatherProbe);
   }

   if (pvalue->m.obstaclePresent) {
      asn1Print_ObstacleDetection ("obstacle", &pvalue->obstacle);
   }

   if (pvalue->m.statusPresent) {
      asn1Print_DisabledVehicle ("status", &pvalue->status);
   }

   if (pvalue->m.speedProfilePresent) {
      asn1Print_SpeedProfile ("speedProfile", &pvalue->speedProfile);
   }

   if (pvalue->m.theRTCMPresent) {
      asn1Print_RTCMPackage ("theRTCM", &pvalue->theRTCM);
   }

   if (pvalue->m.regionalPresent) {
      asn1Print_SupplementalVehicleExtensions_regional ("regional", &pvalue->
         regional);
   }

   rtPrintOpenTypeExtBraceText ("extElem1", &pvalue->extElem1);

   rtxPrintCloseBrace ();
}

void asn1Print_VerticalOffset (const char* name, const VerticalOffset* pvalue)
{
   rtxPrintOpenBrace (name);

   switch (pvalue->t)
   {
      /* offset1 */
      case 1:
      {
         asn1Print_VertOffset_B07 ("offset1", &pvalue->u.offset1);
         break;
      }
      /* offset2 */
      case 2:
      {
         asn1Print_VertOffset_B08 ("offset2", &pvalue->u.offset2);
         break;
      }
      /* offset3 */
      case 3:
      {
         asn1Print_VertOffset_B09 ("offset3", &pvalue->u.offset3);
         break;
      }
      /* offset4 */
      case 4:
      {
         asn1Print_VertOffset_B10 ("offset4", &pvalue->u.offset4);
         break;
      }
      /* offset5 */
      case 5:
      {
         asn1Print_VertOffset_B11 ("offset5", &pvalue->u.offset5);
         break;
      }
      /* offset6 */
      case 6:
      {
         asn1Print_VertOffset_B12 ("offset6", &pvalue->u.offset6);
         break;
      }
      /* elevation */
      case 7:
      {
         asn1Print_Elevation ("elevation", &pvalue->u.elevation);
         break;
      }
      /* regional */
      case 8:
      {
         asn1Print_RegionalExtension ("regional", pvalue->u.regional);
         break;
      }
      default:;
   }

   rtxPrintCloseBrace ();
}

