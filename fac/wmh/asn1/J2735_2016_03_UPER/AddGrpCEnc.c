/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.2.4, Date: 06-Mar-2019.
 */
#include "AddGrpC.h"
#include "rtxsrc/rtxCommon.h"

EXTERN int asn1PE_AltitudeConfidence (OSCTXT* pctxt, AltitudeConfidence value)
{
   int stat = 0;

   RTDIAGSTRM2 (pctxt, "asn1PE_AltitudeConfidence: start\n");

   stat = pe_ConsUnsigned (pctxt, value, 0, OSUINTCONST(15));
   if (stat != 0) return stat;

   RTDIAGSTRM2 (pctxt, "asn1PE_AltitudeConfidence: end\n");

   return (stat);
}

EXTERN int asn1PE_AltitudeValue (OSCTXT* pctxt, AltitudeValue value)
{
   int stat = 0;

   RTDIAGSTRM2 (pctxt, "asn1PE_AltitudeValue: start\n");

   stat = pe_ConsInteger (pctxt, value, -100000, 800001);
   if (stat != 0) return stat;

   RTDIAGSTRM2 (pctxt, "asn1PE_AltitudeValue: end\n");

   return (stat);
}

EXTERN int asn1PE_EmissionType (OSCTXT* pctxt, EmissionType value)
{
   int stat = 0;

   RTDIAGSTRM2 (pctxt, "asn1PE_EmissionType: start\n");

   /* extension bit */

   PU_NEWFIELD (pctxt, "extension marker");

   rtxEncBit (pctxt, 0);

   PU_SETBITCOUNT (pctxt);

   stat = pe_ConsUnsigned (pctxt, value, 0, OSUINTCONST(4));
   if (stat != 0) return stat;

   RTDIAGSTRM2 (pctxt, "asn1PE_EmissionType: end\n");

   return (stat);
}

EXTERN int asn1PE_Altitude (OSCTXT* pctxt, Altitude* pvalue)
{
   int stat = 0;

   RTDIAGSTRM2 (pctxt, "asn1PE_Altitude: start\n");

   /* encode value */

   stat = asn1PE_AltitudeValue (pctxt, pvalue->value);
   if (stat != 0) return stat;

   /* encode confidence */

   stat = asn1PE_AltitudeConfidence (pctxt, pvalue->confidence);
   if (stat != 0) return stat;

   RTDIAGSTRM2 (pctxt, "asn1PE_Altitude: end\n");

   return (stat);
}

EXTERN int asn1PE_PrioritizationResponse (OSCTXT* pctxt, PrioritizationResponse* pvalue)
{
   int stat = 0;
   OSBOOL extbit = FALSE;

   RTDIAGSTRM2 (pctxt, "asn1PE_PrioritizationResponse: start\n");

   /* extension bit */

   PU_NEWFIELD (pctxt, "extension marker");

   extbit = (OSBOOL)(pvalue->extElem1.count > 0);

   rtxEncBit (pctxt, extbit);

   PU_SETBITCOUNT (pctxt);

   /* encode stationID */

   stat = asn1PE_StationID (pctxt, pvalue->stationID);
   if (stat != 0) return stat;

   /* encode priorState */

   stat = asn1PE_PrioritizationResponseStatus (pctxt, pvalue->priorState);
   if (stat != 0) return stat;

   /* encode signalGroup */

   stat = asn1PE_SignalGroupID (pctxt, pvalue->signalGroup);
   if (stat != 0) return stat;

   if (extbit) {

      /* encode extension optional bits length */

      PU_NEWFIELD (pctxt, "ext opt bits len");

      stat = pe_SmallLength (pctxt, pvalue->extElem1.count);
      if (stat != 0) return stat;

      PU_SETBITCOUNT (pctxt);

      /* encode optional bits */

      PU_NEWFIELD (pctxt, "unknown opt bits");

      stat = pe_OpenTypeExtBits (pctxt, &pvalue->extElem1);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      PU_SETBITCOUNT (pctxt);

      /* encode extension elements */

      if (pvalue->extElem1.count > 0) {
         stat = pe_OpenTypeExt (pctxt, &pvalue->extElem1);
         if (stat != 0) return LOG_RTERR (pctxt, stat);
      }
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_PrioritizationResponse: end\n");

   return (stat);
}

EXTERN int asn1PE_PrioritizationResponseList (OSCTXT* pctxt, PrioritizationResponseList* pvalue)
{
   int stat = 0;
   OSRTDListNode* pnode;
   OSUINT32 xx1;

   RTDIAGSTRM2 (pctxt, "asn1PE_PrioritizationResponseList: start\n");

   /* encode length determinant */

   PU_SETSIZECONSTRAINT (pctxt, OSUINTCONST(1), OSUINTCONST(10), 0, 0);

   stat = pe_Length (pctxt, pvalue->count);
   if (stat < 0) return stat;

   /* encode elements */

   xx1 = 0;
   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      stat = asn1PE_PrioritizationResponse (pctxt, ((PrioritizationResponse*)pnode->data));
      if (stat != 0) return stat;

      xx1++;
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_PrioritizationResponseList: end\n");

   return (stat);
}

EXTERN int asn1PE_VehicleToLanePosition (OSCTXT* pctxt, VehicleToLanePosition* pvalue)
{
   int stat = 0;
   OSBOOL extbit = FALSE;

   RTDIAGSTRM2 (pctxt, "asn1PE_VehicleToLanePosition: start\n");

   /* extension bit */

   PU_NEWFIELD (pctxt, "extension marker");

   extbit = (OSBOOL)(pvalue->extElem1.count > 0);

   rtxEncBit (pctxt, extbit);

   PU_SETBITCOUNT (pctxt);

   /* encode stationID */

   stat = asn1PE_StationID (pctxt, pvalue->stationID);
   if (stat != 0) return stat;

   /* encode laneID */

   stat = asn1PE_LaneID (pctxt, pvalue->laneID);
   if (stat != 0) return stat;

   if (extbit) {

      /* encode extension optional bits length */

      PU_NEWFIELD (pctxt, "ext opt bits len");

      stat = pe_SmallLength (pctxt, pvalue->extElem1.count);
      if (stat != 0) return stat;

      PU_SETBITCOUNT (pctxt);

      /* encode optional bits */

      PU_NEWFIELD (pctxt, "unknown opt bits");

      stat = pe_OpenTypeExtBits (pctxt, &pvalue->extElem1);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      PU_SETBITCOUNT (pctxt);

      /* encode extension elements */

      if (pvalue->extElem1.count > 0) {
         stat = pe_OpenTypeExt (pctxt, &pvalue->extElem1);
         if (stat != 0) return LOG_RTERR (pctxt, stat);
      }
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_VehicleToLanePosition: end\n");

   return (stat);
}

EXTERN int asn1PE_VehicleToLanePositionList (OSCTXT* pctxt, VehicleToLanePositionList* pvalue)
{
   int stat = 0;
   OSRTDListNode* pnode;
   OSUINT32 xx1;

   RTDIAGSTRM2 (pctxt, "asn1PE_VehicleToLanePositionList: start\n");

   /* encode length determinant */

   PU_SETSIZECONSTRAINT (pctxt, OSUINTCONST(1), OSUINTCONST(5), 0, 0);

   stat = pe_Length (pctxt, pvalue->count);
   if (stat < 0) return stat;

   /* encode elements */

   xx1 = 0;
   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      stat = asn1PE_VehicleToLanePosition (pctxt, ((VehicleToLanePosition*)pnode->data));
      if (stat != 0) return stat;

      xx1++;
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_VehicleToLanePositionList: end\n");

   return (stat);
}

EXTERN int asn1PE_ConnectionManeuverAssist_addGrpC (OSCTXT* pctxt, ConnectionManeuverAssist_addGrpC* pvalue)
{
   int stat = 0;

   RTDIAGSTRM2 (pctxt, "asn1PE_ConnectionManeuverAssist_addGrpC: start\n");

   PU_NEWFIELD (pctxt, "rsuDistanceFromAnchorPresent");

   rtxEncBit (pctxt, (OSBOOL)pvalue->m.rsuDistanceFromAnchorPresent);

   PU_SETBITCOUNT (pctxt);

   /* encode vehicleToLanePositions */

   stat = asn1PE_VehicleToLanePositionList (pctxt, &pvalue->vehicleToLanePositions);
   if (stat != 0) return stat;

   /* encode rsuDistanceFromAnchor */

   if (pvalue->m.rsuDistanceFromAnchorPresent) {
      stat = asn1PE_NodeOffsetPointXY (pctxt, &pvalue->rsuDistanceFromAnchor);
      if (stat != 0) return stat;
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_ConnectionManeuverAssist_addGrpC: end\n");

   return (stat);
}

EXTERN int asn1PE_IntersectionState_addGrpC (OSCTXT* pctxt, IntersectionState_addGrpC* pvalue)
{
   int stat = 0;
   OSBOOL extbit = FALSE;

   RTDIAGSTRM2 (pctxt, "asn1PE_IntersectionState_addGrpC: start\n");

   /* extension bit */

   PU_NEWFIELD (pctxt, "extension marker");

   extbit = (OSBOOL)(pvalue->extElem1.count > 0);

   rtxEncBit (pctxt, extbit);

   PU_SETBITCOUNT (pctxt);

   PU_NEWFIELD (pctxt, "activePrioritizationsPresent");

   rtxEncBit (pctxt, (OSBOOL)pvalue->m.activePrioritizationsPresent);

   PU_SETBITCOUNT (pctxt);

   /* encode activePrioritizations */

   if (pvalue->m.activePrioritizationsPresent) {
      stat = asn1PE_PrioritizationResponseList (pctxt, &pvalue->activePrioritizations);
      if (stat != 0) return stat;
   }

   if (extbit) {

      /* encode extension optional bits length */

      PU_NEWFIELD (pctxt, "ext opt bits len");

      stat = pe_SmallLength (pctxt, pvalue->extElem1.count);
      if (stat != 0) return stat;

      PU_SETBITCOUNT (pctxt);

      /* encode optional bits */

      PU_NEWFIELD (pctxt, "unknown opt bits");

      stat = pe_OpenTypeExtBits (pctxt, &pvalue->extElem1);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      PU_SETBITCOUNT (pctxt);

      /* encode extension elements */

      if (pvalue->extElem1.count > 0) {
         stat = pe_OpenTypeExt (pctxt, &pvalue->extElem1);
         if (stat != 0) return LOG_RTERR (pctxt, stat);
      }
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_IntersectionState_addGrpC: end\n");

   return (stat);
}

EXTERN int asn1PE_SignalHeadLocation (OSCTXT* pctxt, SignalHeadLocation* pvalue)
{
   int stat = 0;
   OSBOOL extbit = FALSE;

   RTDIAGSTRM2 (pctxt, "asn1PE_SignalHeadLocation: start\n");

   /* extension bit */

   PU_NEWFIELD (pctxt, "extension marker");

   extbit = (OSBOOL)(pvalue->extElem1.count > 0);

   rtxEncBit (pctxt, extbit);

   PU_SETBITCOUNT (pctxt);

   /* encode node */

   stat = asn1PE_NodeOffsetPointXY (pctxt, &pvalue->node);
   if (stat != 0) return stat;

   /* encode signalGroupID */

   stat = asn1PE_SignalGroupID (pctxt, pvalue->signalGroupID);
   if (stat != 0) return stat;

   if (extbit) {

      /* encode extension optional bits length */

      PU_NEWFIELD (pctxt, "ext opt bits len");

      stat = pe_SmallLength (pctxt, pvalue->extElem1.count);
      if (stat != 0) return stat;

      PU_SETBITCOUNT (pctxt);

      /* encode optional bits */

      PU_NEWFIELD (pctxt, "unknown opt bits");

      stat = pe_OpenTypeExtBits (pctxt, &pvalue->extElem1);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      PU_SETBITCOUNT (pctxt);

      /* encode extension elements */

      if (pvalue->extElem1.count > 0) {
         stat = pe_OpenTypeExt (pctxt, &pvalue->extElem1);
         if (stat != 0) return LOG_RTERR (pctxt, stat);
      }
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_SignalHeadLocation: end\n");

   return (stat);
}

EXTERN int asn1PE_SignalHeadLocationList (OSCTXT* pctxt, SignalHeadLocationList* pvalue)
{
   int stat = 0;
   OSRTDListNode* pnode;
   OSUINT32 xx1;

   RTDIAGSTRM2 (pctxt, "asn1PE_SignalHeadLocationList: start\n");

   /* encode length determinant */

   PU_SETSIZECONSTRAINT (pctxt, OSUINTCONST(1), OSUINTCONST(20), 0, 0);

   stat = pe_Length (pctxt, pvalue->count);
   if (stat < 0) return stat;

   /* encode elements */

   xx1 = 0;
   for (pnode = pvalue->head; pnode != 0 && xx1 < pvalue->count; pnode = pnode->next) {
      stat = asn1PE_SignalHeadLocation (pctxt, ((SignalHeadLocation*)pnode->data));
      if (stat != 0) return stat;

      xx1++;
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_SignalHeadLocationList: end\n");

   return (stat);
}

EXTERN int asn1PE_MapData_addGrpC (OSCTXT* pctxt, MapData_addGrpC* pvalue)
{
   int stat = 0;
   OSBOOL extbit = FALSE;

   RTDIAGSTRM2 (pctxt, "asn1PE_MapData_addGrpC: start\n");

   /* extension bit */

   PU_NEWFIELD (pctxt, "extension marker");

   extbit = (OSBOOL)(pvalue->extElem1.count > 0);

   rtxEncBit (pctxt, extbit);

   PU_SETBITCOUNT (pctxt);

   PU_NEWFIELD (pctxt, "signalHeadLocationsPresent");

   rtxEncBit (pctxt, (OSBOOL)pvalue->m.signalHeadLocationsPresent);

   PU_SETBITCOUNT (pctxt);

   /* encode signalHeadLocations */

   if (pvalue->m.signalHeadLocationsPresent) {
      stat = asn1PE_SignalHeadLocationList (pctxt, &pvalue->signalHeadLocations);
      if (stat != 0) return stat;
   }

   if (extbit) {

      /* encode extension optional bits length */

      PU_NEWFIELD (pctxt, "ext opt bits len");

      stat = pe_SmallLength (pctxt, pvalue->extElem1.count);
      if (stat != 0) return stat;

      PU_SETBITCOUNT (pctxt);

      /* encode optional bits */

      PU_NEWFIELD (pctxt, "unknown opt bits");

      stat = pe_OpenTypeExtBits (pctxt, &pvalue->extElem1);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      PU_SETBITCOUNT (pctxt);

      /* encode extension elements */

      if (pvalue->extElem1.count > 0) {
         stat = pe_OpenTypeExt (pctxt, &pvalue->extElem1);
         if (stat != 0) return LOG_RTERR (pctxt, stat);
      }
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_MapData_addGrpC: end\n");

   return (stat);
}

EXTERN int asn1PE_Position3D_addGrpC (OSCTXT* pctxt, Position3D_addGrpC* pvalue)
{
   int stat = 0;
   OSBOOL extbit = FALSE;

   RTDIAGSTRM2 (pctxt, "asn1PE_Position3D_addGrpC: start\n");

   /* extension bit */

   PU_NEWFIELD (pctxt, "extension marker");

   extbit = (OSBOOL)(pvalue->extElem1.count > 0);

   rtxEncBit (pctxt, extbit);

   PU_SETBITCOUNT (pctxt);

   /* encode altitude */

   stat = asn1PE_Altitude (pctxt, &pvalue->altitude);
   if (stat != 0) return stat;

   if (extbit) {

      /* encode extension optional bits length */

      PU_NEWFIELD (pctxt, "ext opt bits len");

      stat = pe_SmallLength (pctxt, pvalue->extElem1.count);
      if (stat != 0) return stat;

      PU_SETBITCOUNT (pctxt);

      /* encode optional bits */

      PU_NEWFIELD (pctxt, "unknown opt bits");

      stat = pe_OpenTypeExtBits (pctxt, &pvalue->extElem1);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      PU_SETBITCOUNT (pctxt);

      /* encode extension elements */

      if (pvalue->extElem1.count > 0) {
         stat = pe_OpenTypeExt (pctxt, &pvalue->extElem1);
         if (stat != 0) return LOG_RTERR (pctxt, stat);
      }
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_Position3D_addGrpC: end\n");

   return (stat);
}

EXTERN int asn1PE_RestrictionUserType_addGrpC (OSCTXT* pctxt, RestrictionUserType_addGrpC* pvalue)
{
   int stat = 0;
   OSBOOL extbit = FALSE;

   RTDIAGSTRM2 (pctxt, "asn1PE_RestrictionUserType_addGrpC: start\n");

   /* extension bit */

   PU_NEWFIELD (pctxt, "extension marker");

   extbit = (OSBOOL)(pvalue->extElem1.count > 0);

   rtxEncBit (pctxt, extbit);

   PU_SETBITCOUNT (pctxt);

   PU_NEWFIELD (pctxt, "emissionPresent");

   rtxEncBit (pctxt, (OSBOOL)pvalue->m.emissionPresent);

   PU_SETBITCOUNT (pctxt);

   /* encode emission */

   if (pvalue->m.emissionPresent) {
      stat = asn1PE_EmissionType (pctxt, pvalue->emission);
      if (stat != 0) return stat;
   }

   if (extbit) {

      /* encode extension optional bits length */

      PU_NEWFIELD (pctxt, "ext opt bits len");

      stat = pe_SmallLength (pctxt, pvalue->extElem1.count);
      if (stat != 0) return stat;

      PU_SETBITCOUNT (pctxt);

      /* encode optional bits */

      PU_NEWFIELD (pctxt, "unknown opt bits");

      stat = pe_OpenTypeExtBits (pctxt, &pvalue->extElem1);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      PU_SETBITCOUNT (pctxt);

      /* encode extension elements */

      if (pvalue->extElem1.count > 0) {
         stat = pe_OpenTypeExt (pctxt, &pvalue->extElem1);
         if (stat != 0) return LOG_RTERR (pctxt, stat);
      }
   }

   RTDIAGSTRM2 (pctxt, "asn1PE_RestrictionUserType_addGrpC: end\n");

   return (stat);
}

